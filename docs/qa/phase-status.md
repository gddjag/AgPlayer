# AgPlayer 阶段验收状态

日期：2026-07-29
分支：`codex/revised-ui`
当前阶段：Phase 6 — 音频工具与导出

## 本阶段已完成

- 格式转换支持真实音频/视频输入、元数据保留、音量标准化、取消、错误恢复及最多 4 路有界并行处理。
- 批量转换在启动前统一预留输出路径；不同目录的同名文件不会覆盖或争用同一输出。
- 轻度剪辑已接通剪切、复制、粘贴、删除、分割、合并、裁剪、撤销、重做和选中片段预览。
- BPM 调速、一键节拍对齐、保持音高、升降调、元数据写入、封面修改及批量重命名均接入真实 C++/FFmpeg 处理路径。
- 四个异步处理控制器在析构时会取消并等待后台任务，避免工作线程访问已销毁对象。
- 音频工具五页完成深色/浅色主题视觉修复；图标随主题着色，升降调和信息修改页不再裁切或重叠。
- 中文、英文、泰语、越南语目录均为 464 条完成翻译，0 条未完成。

## 本轮验证

- Debug 全量构建通过，编译警告按 `/W4 /WX` 门禁处理。
- CTest：44/44 通过，总耗时 42.61 秒。
- 新增真实端到端测试覆盖 FLAC 导出、元数据保持、重名输出、视频提取、BPM 调速、升降调取消、信息写入及批量重命名。
- 音频工具 5 页 × 深浅主题共 10 张生产程序截图已生成；运行日志均为 0 字节。
- 独立复核提出的取消终态与锁定轨道编辑问题已修复；最终无 Critical/Important。
- `git diff --check` 通过。

复现命令：

```powershell
cmake --build build/msvc-debug --parallel 4
ctest --test-dir build/msvc-debug -C Debug --output-on-failure
git diff --check
```

## 尚未完成

- Release 构建、10,000 首完整压力门禁、性能/内存/退出可靠性和安全审计将在 Phase 7 完成。
- 四语言 × 深浅主题全链路截图矩阵及最终发布准备将在 Phase 8 完成。
- 真实声卡/扬声器的可听人工验收仍需用户在场确认。
- 未封装 EXE；仅在全部阶段完成并收到用户明确指令后打包。

## 下一阶段

Phase 7：可靠性、性能、安全与轻量化审计。
