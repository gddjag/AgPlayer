# AgPlayer 阶段验收状态

日期：2026-07-29
分支：`codex/revised-ui`
当前阶段：Phase 4 — 设置、主题与四国语言

## 本阶段已完成

- 设置页改为可滚动的事务式编辑：打开生成快照，取消/关闭完整回滚，保存一次性提交。
- “恢复默认”立即预览默认值，取消仍能恢复打开设置前的状态。
- 深色、浅色、跟随系统三种主题统一使用语义色；静态图标改为可随主题着色的 `ThemedIcon`。
- 修复浅色主题文字对比度、迷你播放器硬编码深色面板和设置卡片重叠。
- 中文、英文、泰语、越南语四套目录均为 460/460 完整翻译；中文也通过 `.qm` 目录加载。
- 新增翻译目录门禁：语言集合、消息数量、空翻译、未完成标记和乱码字符自动检查。
- 新增开发期主题/语言/设置页截图参数，直接走生产 QML 与控制器路径。
- 波形压力测试保留 10,000 首手动模式；常规 CTest 使用 64 首完整解码/缓存样本，避免每轮占用数分钟。

## 本轮新鲜验证

- 全量 Debug 构建：通过；零编译错误。
- 全量测试：43/43 通过，总耗时 27.65 秒。
- 波形解码/缓存压力烟测：64 首通过，2.97 秒。
- 10,000 曲模型压力测试：通过，0.75 秒。
- 翻译目录：四种语言各 460 条，0 未完成、0 空翻译、0 乱码替换符。
- 四组生产 EXE 截图：中文深色、英文浅色、泰语深色、越南语跟随系统均成功。
- 四组运行日志均为 0 字节；截图人工目检无乱码、方框字、控件重叠或不可辨识图标。
- `git diff --check`：通过。

复现命令：

```powershell
cmake --build build/msvc-debug --parallel 8
ctest --test-dir build/msvc-debug -C Debug -E "^library(_model)?_stress_test$" --output-on-failure
ctest --test-dir build/msvc-debug -C Debug -R "^library(_model)?_stress_test$" --output-on-failure
git diff --check
```

## 尚未完成

- 真实声卡/扬声器的可听人工验收仍需用户在场确认；自动化已证明播放状态和时间推进。
- 窗口磁吸、双窗口联动、位置恢复和迷你播放器完整行为进入下一阶段。
- 五个音频工具仍按后续阶段验收。
- 未封装 EXE；仅在全部阶段通过且用户明确要求后打包。

## 下一阶段

Phase 5：主播放器/歌单窗口四边磁吸、拖离、隐藏/最小化联动、位置恢复与迷你播放器一致性。
