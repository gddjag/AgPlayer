# AgPlayer 沉浸视觉 V4.6 视觉 QA

## 验收输入

- 动态目标：`E:/Administrator/下载/微信视频2026-08-29_170235_907.mp4`，32.35 秒，1278×852，约 33.8 FPS。
- 最终 HTML：`C:/Users/Administrator/Desktop/音视频播放器/AgPlayer_Immersive_Visual_Reactor_V4_6_Codex_Ready.html`。
- 用户提供的两张 V4.6 截图。
- 原生实现固定视口：2169×1131。

## 同输入对比结论

原生实现与参考图已放入同一张 2169×1131 对比输入检查，而不是只检查单张实现截图。

| 维度 | 结论 |
|---|---|
| 构图 | 自动旋转保留，默认视线目标与最终 HTML 的俯视构图对齐；中央峰群、外圈衰减和左侧面板比例已再校准 |
| 地形密度 | 160×160 主网格不变；外围闲置高度按响应场衰减，减少无意义粗颗粒；中央用连续簇状峰与稀疏高峰混合，保留“连续地形 + 城市峰群” |
| 音乐响应 | 中心亮度、体素高度、彩色冲击波、漂浮体/流星均由现有频谱或节拍特征驱动 |
| 色彩 | 青蓝、珊瑚、粉红、暖白形成稳定逐曲调色板；环带着色和中央亮度有独立真实反馈；手动色块会覆盖实际渲染参数 |
| 镜头 | 拖动、滚轮、自动旋转、节拍冲击及 4 秒恢复均接入相机状态 |
| 波形 | 使用播放器原 `WaveformItem` 与既有 mix/bass/mid/high 峰值；频彩模式以低/中/高能量混色，透明无边框，不新增整曲解码或 FFT |
| 歌词 | 支持显示开关、左/中/右、水平/垂直、大小、清晰度、纵深和透明度，均写入实际布局/变换 |
| 面板 | 六预设 3×2、歌词与动态参数、颜色选择器、质量/宿主控制均可操作 |

## 可见差异与裁定

- 参考 HTML/视频仍具有更强的多通道 Bloom、发白光带和大范围空气雾化。当前实现增加一层 Qt 自带的受控模糊采样，但桌面 Eco 模式关闭柔光，窗口/全屏也保持比参考更克制。
- 当前剩余差异是轻量原生方案的资源边界，不属于控件空壳或数据未连接。所有滑杆/开关仍写入实际渲染快照或相机状态。
- 动态视觉按结构、节奏和感知一致性验收，不声称与某一静态帧逐像素一致。

## 视觉证据

- `build/qa/native-visual/immersive-frequency-soft-final-v8-2169x1131.png`
- `build/qa/native-visual/reference-vs-frequency-soft-final-v8-2169x1131.png`
- `build/qa/native-visual/immersive-v46-video-audio-2169x1131.png`
- `build/qa/native-visual/settings-frequency-color-waveform.png`
- 视频抽帧目录：`build/qa/reference-v46-video/`

## 环境限制

本次只完成 Windows 当前缩放/显示环境的运行视觉验收。DPI 矩阵、macOS、Linux、设备丢失恢复和 30 分钟运行未执行，不标记为通过。
