# AgPlayer 沉浸视觉 V4.6 视觉 QA

> [!IMPORTANT]
> **2026-08-31 覆盖性说明**：此前上传的 HTML、截图、图片和视频现仅作为历史输入，不再是最终视觉目标，也不得用于本轮“相似度”或完成验收。当前视觉 QA 只检查 AgPlayer 原创 Qt/QML/QRhi 实现是否满足内部定义、可复现且有界的产品语义。开源仓库仅可 study-only；不得复制、翻译或改写其源码、Shader、算法、常量、参数表、布局结构或资产。

## 2026-08-31 当前视觉验收口径

| 维度 | 当前内部标准 | 2026-08-31 最终证据 |
|---|---|---|
| 舞台轮廓 | 反应区域呈圆形并向深色环境自然衰减，不出现可辨识的方形网格边界。 | **PASS，仅固定合成频谱/D3D11**：最终 `1600×900` 原生截图内部检查确认圆形占用与方界消隐；没有与上传媒体比较。 |
| 地形层次 | 低频负责宽中心重量，中频形成连续宽脊；禁止由稀疏随机值制造主导性的粗糙孤塔。 | **PASS，自动化 + 内部截图检查**：最终 GPU 完整用例 `8/8`；内部检查环境密度与中心层次通过。 |
| 高频语义 | 高频以稀疏顶面流光和受控细节为主；同强度输入的最强 1% 相对响应不超过低/中频的 90%，流光不得退化为全场曝光提升。 | **PASS，仅 Windows D3D11**：Task 2 最强 1% 比例约 `31.6%–32.1%`；高光 GREEN 近白 `0%`、P90/P95/P99 `176/190/207`。 |
| 光影与环境 | 顶面比侧面明亮且具有局部流动；侧面稳定、较暗；中心高光、冲击、粒子、流星和漂浮体均使用有界响应。 | **PASS，固定合成频谱**：高光 RED 近白 `2.20%`、P90/P95/P99 `201/231/253`，修复后 GREEN 为 `0%`、`176/190/207`；最终截图中心细节与颜色保留通过内部检查。 |
| 相机 | 拖动、滚轮、自动旋转和节奏冲击保持有限状态；NaN/Inf 输入不得污染公开属性或 GPU uniform。 | **自动化 PASS；人工 NOT RUN**：最终 Release focused 通过，Debug 相机相关 C++/GPU 项通过；未人工执行拖动、滚轮、全屏 Esc。 |
| 电影感歌词 | 当前行唯一聚焦，前/后行弱化；左/右透视镜像且有界，中央正面；最多两行并留在真实 surface 安全区内，快速切行或替换服务无残留动画。 | **Release 自动化 PASS；Debug harness 间歇不稳**：最终 Release 六项 focused `6/6`；Debug 的 `qml_immersive_integration_test` 在 CTest 无输出失败，严格同环境隔离直跑一次 exit `0`，不记 Debug 全绿。 |
| 设置反馈 | 地形 / 光影 / 运动 / 冲击四组中的 9 个滑杆和 8 个开关必须继续写入原有 controller/renderer 属性并产生真实反馈。 | **Release 自动化 PASS**：Task 3 独立评审 PASS，最终 Release focused `6/6`；本轮没有逐个滑杆进行人工视觉验收。 |
| 共享能力 | 频彩波形、队列和歌词服务/计时的数据链保持不变；不得新增播放器、解码器、FFT 或长期音频缓存。 | **代码范围 PASS；真实音频 NOT RUN**：高光修复未改这些链路，清洁扫描无第二解码器/新 FFT/新依赖；合成 QA 没有真实曲目，未验证频彩波形的真实音频观感。 |

Task 1–3 报告中的局部 RED/GREEN、D3D11 GPU 指标和 QML 安全区结果与最终 HEAD 的 focused/QA 证据共同构成范围内验收；它们不能覆盖完整 Release `118/121`、完整 Debug `102/122` 的失败。最终视觉证据使用固定种子与合成频谱，没有重新引入上传媒体对照。

## 2026-08-31 最终证据 / 限制

- 最终验证 HEAD：`6cc4bab`。中心高光修复为 `2b6c80f`，度量稳定化为 `6cc4bab`；独立复审 PASS，无 P0/P1/P2。
- 实际 Release QA 进程以 `--qa-immersive --qa-immersive-synthetic --qa-width 1600 --qa-height 900` 启动，exit `0`，退出后无本次进程残留。截图：`build/qa/immersive-reactor-highlight-2b6c80f/immersive-synthetic-1600x900.png`，`562598` bytes。
- 高光测试执行 TDD：RED 近白占比 `2.20%`、P90/P95/P99 `201/231/253`；GREEN `0%`、`176/190/207`。完整 GPU `8/8`；Release follow-up repeat `5/5`，Debug 通过。
- 60 秒 GPU CTest 专用看门狗提交 `5b17b63` 后 repeat `10/10`，最长 `13.48 s`。此前 10 秒配置第 4 次 `11.04 s` 超时，隔离 `4.73 s` 通过；旧配置不记为 `10/10`。
- 旧版 AgPlayer 内部 QA 帧到最终帧（并非上传媒体对比）：可见近白 `9.41%→3.21%`，中央近白 `11.19%→3.54%`，P90 `247→224`，亮区颜色保留约 `42.09%→62.7%`。内部判定圆形轮廓、方界消隐、环境密度和中心层次 PASS。
- 完整 Release CTest 为 `118/121`，完整 Debug 为 `102/122`，均是 FAIL。范围内 focused Release `6/6` 通过；Debug 五个 C++/GPU 项通过，沉浸 QML 在 CTest 间歇失败、严格隔离直跑一次 exit `0`。
- 自动截图不是人工交互验收：未使用真实音频验证频彩波形，也未人工验证进入/返回、全屏 Esc、鼠标拖动/滚轮或全部参数手感。

## 当前验证边界

- Windows Direct3D 11 / NVIDIA GeForce RTX 4070 Ti SUPER 是唯一具有最终加速图像证据的后端；Metal、Vulkan、OpenGL 均未验证。
- macOS/Linux、Windows 全 DPI/分辨率矩阵、设备丢失/恢复、50 次宿主切换和 30 分钟 soak 均未执行。
- 本轮不宣称与任何上传画面“达到同样感觉”或逐像素一致；验收对象是上述原创结构、响应语义、可读性和资源边界。
- QA 与生产设置分离，但 QSettings / pipeline cache 仍落在系统 `qttest` 命名空间；不能声称所有 Qt 状态都隔离在一次性目录。

---

## 2026-08-29 至 2026-08-30 历史视觉 QA（不再是当前目标）

以下同输入对比、参考差异和证据路径按原样保留用于追溯当时工作，不得用来覆盖 2026-08-31 的当前验收口径。

## 验收输入

- 动态目标：`E:/Administrator/下载/微信视频2026-08-29_170235_907.mp4`，32.35 秒，1278×852，约 33.8 FPS。
- 最终 HTML：`C:/Users/Administrator/Desktop/音视频播放器/AgPlayer_Immersive_Visual_Reactor_V4_6_Codex_Ready.html`。
- 用户提供的两张 V4.6 截图。
- 原生实现固定视口：2169×1131。

## 同输入对比结论

原生实现与参考图已放入同一张 2169×1131 对比输入检查，而不是只检查单张实现截图。

| 维度 | 结论 |
|---|---|
| 构图 | 自动旋转保留，默认视线目标与最终 HTML 的俯视构图对齐；中央峰群、外圈衰减和左侧面板比例已再校准 |
| 地形密度 | 160×160 主网格不变；外围闲置高度按响应场衰减，减少无意义粗颗粒；中央用连续簇状峰与稀疏高峰混合，保留“连续地形 + 城市峰群” |
| 音乐响应 | 中心亮度、体素高度、彩色冲击波、漂浮体/流星均由现有频谱或节拍特征驱动 |
| 色彩 | 青蓝、珊瑚、粉红、暖白形成稳定逐曲调色板；环带着色和中央亮度有独立真实反馈；手动色块会覆盖实际渲染参数 |
| 镜头 | 拖动、滚轮、自动旋转、节拍冲击及 4 秒恢复均接入相机状态 |
| 波形 | 使用播放器原 `WaveformItem` 与既有 mix/bass/mid/high 峰值；频彩模式以低/中/高能量混色，透明无边框，不新增整曲解码或 FFT |
| 歌词 | 支持显示开关、左/中/右、水平/垂直、大小、清晰度、纵深和透明度，均写入实际布局/变换 |
| 面板 | 六预设 3×2、歌词与动态参数、颜色选择器、质量/宿主控制均可操作 |

## 可见差异与裁定

- 参考 HTML/视频仍具有更强的多通道 Bloom、发白光带和大范围空气雾化。当前实现增加一层 Qt 自带的受控模糊采样，但桌面 Eco 模式关闭柔光，窗口/全屏也保持比参考更克制。
- 当前剩余差异是轻量原生方案的资源边界，不属于控件空壳或数据未连接。所有滑杆/开关仍写入实际渲染快照或相机状态。
- 动态视觉按结构、节奏和感知一致性验收，不声称与某一静态帧逐像素一致。

## 视觉证据

- `build/qa/native-visual/immersive-frequency-soft-final-v8-2169x1131.png`
- `build/qa/native-visual/reference-vs-frequency-soft-final-v8-2169x1131.png`
- `build/qa/native-visual/immersive-v46-video-audio-2169x1131.png`
- `build/qa/native-visual/settings-frequency-color-waveform.png`
- 视频抽帧目录：`build/qa/reference-v46-video/`

## 环境限制

本次只完成 Windows 当前缩放/显示环境的运行视觉验收。DPI 矩阵、macOS、Linux、设备丢失恢复和 30 分钟运行未执行，不标记为通过。

## 2026-08-30 最终 HTML 独立窗口复核

- 新对比输入统一为 1900×939：上半部为用户最终 HTML 参考截图，下半部为固定种子、固定合成频谱的 Qt/QRhi 原生窗口。
- 可见结构已按后续要求更新：移除左上角 `AgPlayer · 沉浸视觉`，右上只保留图标式全屏操作；反应堆不再嵌在双窗口或单窗口播放器页面。
- 地形由宽中心穹顶、连续空间簇峰和受控单柱差异组成；随机种子不再直接制造粗糙孤立尖塔。中心亮度、宽冲击高光、彩色环、漂浮体和流星仍由现有音频特征驱动。
- 同输入检查仍能看到参考 HTML 的后期 Bloom 和空气雾化更强；原生版本保持更轻的 Qt 自带模糊层和较清晰的体素边缘，不声明逐像素一致。
- 本地证据：`design-qa/immersive-final/native-reference-viewport-v10.png`、`design-qa/immersive-final/reference-vs-native-v10.png`。这些是开发工作树内的 QA 产物，不进入安装包。

## 2026-08-30 圆形舞台与独立主题复核

- 最终对比使用用户提供的最终 HTML 截图，按覆盖裁切统一到 2160×981；左侧为参考，右侧为同尺寸固定种子/固定合成频谱的 Qt/QRhi 原生截图。
- 可见改进：舞台实例边界为真实圆形；外围改为深黑星空并扩大留白；活跃核心收拢；外圈体素亮度和透明度渐隐；顶面流光从整片发白改为少量柱体随高频流动闪烁。
- 独立窗口右上角显示返回窗口主题与全屏图标；主播放器和歌曲列表不再保留在沉浸窗口背后。窗口恢复行为由自动测试证明，不以单张截图替代。
- 动态页结构已与最终 HTML 对齐为 9 个滑杆、8 个开关和 5 个实时语义读数。五项读数由原生反应堆实际八段频谱与 kick 特征绑定，低/高频占比和锐度、平滑度、密度含义与最终原型一致；kick 对锐度和密度的贡献采用连续衰减包络而非布尔跳变。歌词、队列和底部频彩波形保持原有实现，本轮未改其布局或数据链。
- 仍有感知差异：HTML 的浏览器后处理 Bloom 与空气雾化更强，原生版本保留更清晰的体素边缘和更克制的柔光。当前证据支持结构和感知方向明显接近，不支持“逐像素一致”结论。
- 本轮最终证据：`design-qa/2026-08-30-immersive-revision-current-f.png`、`design-qa/2026-08-30-html-reference-vs-native-final.png`；运行日志为 `build/qa/immersive-revision-runtime-f.log`，没有 QML warning/error。
