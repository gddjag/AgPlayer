# AgPlayer 最新 UI 开发计划

**目标：** 依据 2026-07-26 提供的播放器、歌单和空白启动页设计稿，保留现有 Qt 6/C++17 播放内核与真实交互。

**基线：** `codex/revised-ui` 分支，基于 `30c3f26`，隔离工作区为 `D:\ai\AgPlayer\.worktrees\revised-ui`。旧的脏工作区 `phase-1-playback` 保持不动。

**规则：** 行为变更先写测试；复用现有控制器、模型与 QML；非必要不加依赖；启用 `/W4 /WX`；先跑聚焦测试，再跑全量回归；每个 UI 阶段做同尺寸视觉对比；不封装 EXE。

## 阶段 0：基线与安全

- [x] 使用 codebase-memory-mcp 索引干净基线。
- [x] 确认 MSVC Debug 构建通过。
- [x] 确认原有 28 个原生测试和 4 个 QML 测试通过。
- [x] 以缩小数据量执行日常压力回归；完整 10,000 首测试留到最终性能门禁。

## 阶段 1：空白启动页与真实导入

- [x] RED：测试文件夹递归导入、音频扩展名过滤及既有去重路径。
- [x] GREEN：以 Qt 文件系统 API 实现最小的 `ImportController::importFolder(QUrl)`，复用 `importUrls`。
- [x] RED：测试空库时显示启动页及“打开文件”“导入文件夹”两个操作。
- [x] GREEN：空库时显示新启动页，两个按钮连接真实文件/文件夹选择器。
- [x] 聚焦测试、非压力全量回归、缩小数据量压力回归通过。
- [x] 以 1540×712 同尺寸对比 `空白启动页.png`，修正窗口联动及主要可见偏差。

## 阶段 2：主播放器视觉还原

- [ ] 按 `播放页确定.jpg` 重排封面、元数据、星级、RGB 波形、时间轴、控制区、音量及快捷入口。
- [ ] 所有可见操作继续连接既有控制器，清除可见占位行为。
- [ ] 为改动控件补齐键盘焦点与无障碍名称。
- [ ] 同尺寸比较设计稿和实现，清除 P0/P1/P2 视觉差异。

## 阶段 3：歌单窗口与筛选栏

- [ ] 将搜索、星级、BPM 区间、清空和菜单移至设计稿底栏。
- [ ] 搜索继续复用 C++ 内存代理模型，匹配歌名、艺术家、专辑。
- [ ] BPM 滑块加入 200 ms 防抖、60–160 手动输入及两个独立滑块。
- [ ] 增加匹配文本强调、准确空结果提示和一键清空。
- [ ] 还原列顺序、紫色选中行、侧栏以及新建/导入操作。
- [ ] 验证 10,000 行模型保持虚拟化和响应性。

## 阶段 4：双窗口磁吸联动

- [ ] 保留四边 15 px 磁吸和默认下方位置。
- [ ] 吸附时拖动主窗口带动歌单；拖动歌单可拆离。
- [ ] 双击主窗口标题栏拆离歌单。
- [ ] 主窗口最小化、隐藏、关闭与歌单同步；恢复时保留吸附边。
- [ ] 设置页上下左右选项复用同一套几何逻辑。

## 阶段 5：迷你播放器、设置和音频工具

- [ ] 依据参考图复核迷你播放器，不创建第二套播放状态。
- [ ] 将 `设置.txt` 条目映射到现有 `SettingsController`，仅补已确认缺失项。
- [ ] 保持格式转换、剪辑、BPM 调速、升降调和元数据工具可用。
- [ ] 每个改动控制器或工具执行聚焦功能测试。

## 阶段 6：发布质量门禁

- [ ] Debug、Release 均零警告零错误。
- [ ] 原生与 QML 全量回归通过。
- [ ] 10,000 首完整压力测试在 600 秒门禁内完成。
- [ ] 真实设备播放、定位、无缝播放、波形、内存和格式矩阵通过。
- [ ] 同视口、同状态完成视觉比较，`design-qa.md` 最终结果为 passed。
- [ ] 明确记录：未封装 EXE。
