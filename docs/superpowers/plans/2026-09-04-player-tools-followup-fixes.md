# AgPlayer 播放器与音频工具跟进修复计划

> 执行基线：`main@ee7126657b627d1f2bbd2a301a50e27c3bcd19f2`，隔离工作树 `codex/ui-runtime-followup-20260904`。

## 目标与边界

- 修复双窗口、迷你和滚动播放器的波形尾部、密度、拖动与控制条问题。
- 统一单窗口/滚动窗口共享列表的列间距、伸缩、缩略图高度和筛选栏位置。
- 修复人声伴奏分离的波形分析、运行时配置入口及五轨模型失败诊断。
- 复现并修复元数据真实文件写入失败，继续保留暂存、验证、备份和原子替换安全约束。
- 按参考图调整格式转换并发控件，并收窄、增高沉浸视觉控制面板。
- 不重写播放核心，不引入重型 3D/推理依赖，不把未验证模型标记为可用。

## 执行步骤

### 1. 建立可复现基线

- [ ] 从隔离工作树全新配置并构建 Release 全目标。
- [ ] 运行当前 163 项测试，区分旧构建目录问题与源码回归。
- [ ] 为本轮问题记录最小复现：尾部非零峰值、滚动音量、共享列布局、分离波形、元数据格式矩阵。

### 2. 波形与播放器

- [ ] 先补充尾部峰值、缩放/DPI 与滚动视口密度测试，并确认修改前失败。
- [ ] 让双窗口和迷你播放器使用同一内容宽度与末端采样映射，最后有效峰值落在可见物理像素内。
- [ ] 滚动视口按固定时间范围截取原始峰值密度，禁止把稀疏采样放大铺满；保留 8 拍默认视口。
- [ ] 为滚动平移加入轻量帧节流/插值，松手后以中心针时间定位并继续播放。
- [ ] 修复滚动音量滑条显示；将主题、沉浸、迷你按钮垂直居中并放在缩放重置右侧。

### 3. 共享列表、筛选与标签

- [ ] 为集成/滚动共享布局增加窄、默认、宽尺寸契约测试。
- [ ] 使用同一列间距规则；时长/评分/收藏固定，标题和波形按可用宽度共同增长。
- [ ] 缩略图高度低于封面并垂直居中；标题、副标题、波形及尾列统一对齐封面中心。
- [ ] 双窗口筛选栏增加底部留白；共享列表筛选栏减少外层底边距、贴近底部但不碰边。
- [ ] 标签胶囊圆角小幅提高并保持内容驱动宽度。

### 4. 音频工具

- [ ] 格式转换并发控件移至底栏右侧状态/操作区，并验证紧凑布局不溢出。
- [ ] 分离页补充输入与输出波形代次测试，修复路径/trackId 不一致导致的结果丢弃。
- [ ] 本地模型使用“配置运行时”动作而不是模型下载；提供可执行的一键配置、明确支持类型和环境说明。
- [ ] 为标准五轨模型补充加载/张量校验/推理阶段错误与超时回归测试，避免无响应。
- [ ] 用 MP3、FLAC、WAV、M4A/AAC、OGG 中文路径矩阵复现元数据失败；只放宽确认属于容器正规化的验证，不绕过音频等价检查。

### 5. 沉浸视觉与主题验收

- [ ] 控制面板收窄并增加可用高度，内容超出时保持内部滚动与对齐。
- [ ] 检查深浅主题下本轮调整控件的 Token、边框、文字与悬停状态。
- [ ] 运行界面并截图验收双窗口、单窗口、滚动、分离、格式转换和沉浸控制面板。

### 6. 验证与发布

- [ ] 运行相关 Debug/Release 单测、QML 测试、QML 静态检查及全量 Release 测试。
- [ ] 执行最终 `git diff --check` 与逐文件 Diff Review。
- [ ] 提交功能分支，快进或合并到 `main`，重新构建安装包。
- [ ] 将唯一最新版安装包复制到桌面，真实启动/导入/播放冒烟，并记录提交、版本、大小与 SHA-256。

