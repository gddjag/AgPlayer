# v1.0.0 含沉浸视觉的人工测试集成

## 范围

- 主线六项反馈修复：`18bb37f`。
- 沉浸视觉原生材质、内部光照、受控柔光、阴影与故障降级：`65440aa`。
- 用户明确授权合并全部成果、提交并重新打包 EXE 人工测试；不推送远程、不发布网站。
- 产品版本保持 1.0.0，不将新测试包误升为 1.0.1。

## 分支审计

已登记的其他分支提交均已进入 main。除 main 和沉浸视觉工作树外，检查 34 个工作树，27 个干净，7 个有旧快照。

- full-ui-performance / lossless-identification 已通过 `792b732` 等历史提交按块整合，后续主线继续演进；不以旧目录覆盖新实现。
- lossless-baseline / immersive-visual-lyrics 为已整合或已替代的对照实现。
- task8 性能门禁和单声道时间线由 `88bec90` / `2824f83` 明确回退，不重新引入。
- task7 是未完成的旧 RED 测试，没有独立生产成果。
- 所有旧工作树保留；根目录原有 `'-'` 是历史测试日志，不是代码，不纳入安装包。

## 集成前发现与验证

- 主线 Release 全套首轮 176/181。scratch/settings/single-instance 串行复测通过。
- 专业模式离屏文字宽度断言失败：未加载 Windows 字体，六字符占84px；指定系统字体目录恢复44px。修正测试环境，未改生产字号、尺寸或放宽断言。
- 分离排队探测可能在验证期间再次排队并覆盖 Completed；阻止 Running/Cancelling 阶段的后台探测。controller/rolling 最终串行2/2通过。
- 沉浸视觉新增阴影采样失败降级：SRB/主管线故障后释放资源，切白色备用纹理只重试一次。故障注入先RED，完整材质17/17通过。
- 补齐4条新增沉浸参数/预设中英文文案。

## 人工测试与发布边界

本包含最新沉浸视觉实现，不代表已完全达到参考图的柱体通透感、发光顶沿或完整动态规格。九预设逐项视觉、长期稳定性、全部DPI、macOS/Linux及真实大型模型GPU分离未在此轮完整验收。

无损鉴别继续明确为“实验性，仅供参考”。干净系统安装/卸载、人工听音、签名与公开分发许可安排仍是正式上架前事项。本机测试包不等同正式发布达标。

## 合并版验证

- 完整 Release 构建通过：`build/qa/immersive-integration-release-build.txt`。
- 全量184项首轮181通过：`immersive-integration-full-tests.txt`。保留3项首次失败，不描述为首次全绿。
- 主窗口独立完整复测141通过/1跳过：`integrated-main-diagnostic.txt`。
- 真实字体下无损详情超出16px，压缩局部留白而不减少内容/字号；修复后无损/转换/元数据/分离QML 4/4通过：`integration-layout-recheck.txt`。
- 沉浸GPU完整自然结束31通过/1可选性能探针跳过，92.63秒：`integrated-terrain-gpu-direct.txt`。只将整套外层看门狗90→120秒，内部断言、等待和帧预算均未调整，不把此项视作60FPS性能保证。
- 新增材质、几何与原生柔光测试在首轮均通过；沉浸跨宿主/资源生命周期QML通过。
- 最终 CTest GPU 复测通过，94.98秒：`integration-gpu-final-ctest.txt`。首轮三项失败均已完成针对性复测；未把复测描述为重新执行全部184项。
