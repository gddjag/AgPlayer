# macOS 反应堆卡帧与节奏排查

工作区 `macos-sync-20260920`，基线 `eae61589`。用户反馈：播放期间持续不流畅、顶起不跟拍；设备为 2014 年 Intel i7 2.5 GHz / NVIDIA 750M。未取得当前 Mac 测试包标识或实机帧时间，不能将问题直接归因于硬件。

后续硬件更正：用户确认 750M 已不工作，目前仅使用核显（系统显示 1536 MB），测试时风扇持续高速运转。

## Windows 对照

与 `pc-six-track-editor`（检查时 HEAD `29db0c06`）比较，反应堆 item、音频特征控制器、音频帧分析器、频谱/地形响应、snare 分析及地形顶点/片元着色器的核心路径一致；QML 沉浸面板无语义差异。Windows 工作区另有进行中的未提交修改，未写入该工作区。

Windows 本地 Qt 为 6.7.0 / D3D11；Mac 工作流使用 Qt 6.8.3。源码一致不能证明两个设备的 GPU 负载、运行时帧率或当前安装包一致。

## 已实施的局部修复

1. `TerrainShadowMap::configure` 使用与片元着色器 `referenceMode` 相同的条件，跳过标准主题不读取的地形阴影绘制。绑定的深度纹理仍初始化，自定义材质仍可恢复阴影。不改变网格密度、分辨率、材质、音频增益和主题颜色。
2. 定时刷新切换到逐帧刷新时，在停掉定时器前请求首帧。此前 Balanced -> High 在采样数/目标不变时可能停止绘制。回归测试移除窗口 resize，防止无关刷新掩盖缺陷。

## 验证

- 复用本工作区忽略目录下的局部 CMake 工程和现有 Qt/音频依赖缓存，增量构建两个 Release 测试目标。未进行完整应用构建或 macOS 构建。
- 新阴影回归在修改前 3 个数据行均失败于“阴影仍启用”，但输出 RGB 和轮廓已经一致；修改后全部通过。覆盖运行时主题、参考回放和无显式波纹颜色的参考舞台，并验证切换自定义材质/返回主题不重建资源。
- 材质定向用例 5/5：上述 3 行，加真实自定义阴影/透明度边界。日志：`build/reactor-shadow-before.txt`、`build/reactor-shadow-after.txt`。
- 调度测试修改前在最后一次 High 切换失败；修复后通过。GPU 定向用例 5/5：画质/密度切换、真实 PCM 分析与隐藏停止、三个主题的顶起图像。日志：`build/reactor-switch-red.txt`、`build/reactor-gpu-final.txt`。
- 图像位于 `build/import-regression-native/Release/{violet-heart,sakura-glow,abyss-blue}-{quiet,raised}.png`。这是 Windows GPU 证据，不是 Mac 实机验收。
- 3840×2160 / DPR 2 / 24,025 柱体的本机旧探针结果没有显示稳定帧率改善；该探针用 QTest::qWait 驱动事件，不能用来推导真实应用帧率或 Mac 提升比例。本次仅声称消除无效绘制，不声称帧率提升。

## 节奏诊断与待确认方案

现有 `VisualAudioFrameAnalyzer` 每绘制一帧执行一次。FFT 和频谱特征平滑使用固定的每帧系数，顶起峰值检测同样按帧推进。低帧率会改变时间响应；仅同步相同源码不能消除这种差异。

一次性诊断入口 `build/import-regression-src/reactor_cadence_probe.cpp` 直接调用现有分析器与地形响应，用 48 kHz、70 Hz 指数衰减鼓点、120 BPM、36 拍，排除最初 4 拍，比较不同绘制频率。衰减参数 35 和 70 均得到：

| 模拟绘制帧率 | 匹配鼓点 | 平均检测延迟 |
| --- | --- | --- |
| 60 FPS | 32/32 | 33.3 ms |
| 30 FPS | 32/32 | 66.7 ms |
| 20 FPS | 32/32 | 100.0 ms |
| 15 FPS | 16/32 | 100.0 ms（仅已检测拍） |

日志 `build/reactor-cadence-results.txt`。这是受控合成 PCM 诊断，不是用户 Mac 的测量，也不能代表任意曲目准确率。

标准主题 High 分支还会覆盖自动降载的分辨率、网格和采样预算。本次未改此策略，避免未经实机验证降低画质。

已向用户提出：以 Windows 60 帧的分析节奏为基准，将真实音频分析与绘制频率解耦，保留主题、播放核心及现有依赖。因为这改变分析调度，等待用户确认后实施；当前未修改鼓点检测算法/调度，不能声称已解决 Mac 跟拍问题。

用户要求先本地提交已完成修复，留待下次更新发布；本次不推送、打包或发布，发布仍由“版本发布”任务负责。分析与绘制解耦尚未实施，本次提交授权不视为批准该架构调整。两处原有 Python 缓存保留，不纳入提交。

## 后续：按 Windows 逻辑修复低配降载

用户随后要求“沉浸反应堆对齐 win 就行，优化一下低配电脑的运行速度”。保留当前与 Windows 一致的 PCM 分析、鼓点判断和地形响应，只修复渲染预算；没有实施先前提出的分析调度解耦。

- 标准主题 High 原先无条件覆盖自动降载的漂浮物、流星、波纹、网格、分辨率和 MSAA 设置。现在保留完整画质默认值，持续超预算后服从现有自动降载及恢复策略。
- 参考波纹着色器原先始终遍历十个槽。现在遵守实际波纹预算，鼓点及鼠标波纹也写入有效槽位，避免降低预算后将新事件写到不绘制的槽中。
- 增量构建两个现有 GPU 测试目标通过。新增持续慢帧回归在修改前失败（39,204 柱始终不降），修改后降至 9,216 柱，渲染缓冲从 640×360 降至 448×252，MSAA 从 4 降至 1，且继续绘制。慢帧由测试在呈现路径注入 40 ms 延迟，不代表真实 Mac 帧率。
- 波纹回归在修改前失败（预算降低后第十槽仍绘制），修改后通过；完整预算的第一/第十槽输出相同，低预算的有效槽仍正常绘制，普通/白色波纹及开关保持原有契约。
- 本次定向测试共 10 个实际用例通过（GPU 9 + 材质 1，不计初始化和清理），包括三主题顶起、普通鼓点波纹、流星落地白波、画质切换及隐藏后停止分析。日志：`build/reactor-adaptive-red.txt`、`build/reactor-adaptive-green.txt`、`build/reactor-ripple-red.txt`、`build/reactor-ripple-green.txt`。
- 以上为 Windows D3D11 局部验证。本轮未重新构建 macOS 包、未实测旧核显、未证明漏拍消失或量化流畅度/风扇改善；此前交付的专项包不包含这轮降载修复。
