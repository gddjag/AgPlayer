# 人工测试版集成交付

用户已授权先整合全部冻结成果并打包人工测试，不等待无损鉴别达到专业准确度目标。本次为测试候选，不是专业验收通过版本。

## 范围

- 主线既有播放器、音频工具、UI/性能、废弃功能清理及频彩最新修复。
- 沉浸视觉 `f615e023` 之上的冻结工作树增量；三方合并，不覆盖主线的布局与退出保护。
- 无损鉴别 `lossless-1.4 / lossless-params-7` 完整候选；来源补丁 SHA-256：`A6D14C274791CB82BB0458AAC9D1F0DECC580938D440D7D9C691F780AC69D49C`。

## 已知限制

无损鉴别的专业准确度目标未完成。params6 的历史新来源盲测有损识别为 103/160，FIR 反例误报 2/20；params7 移除了证据不足的判定规则，不能沿用 params6 数字当作最终准确率，也没有新的完整独立盲测结果。高码率 MP3、部分 Opus、复合编码链及置信度校准仍不充分。鉴别结果仅供测试参考，不应作为音源真假或质量的唯一依据。

大型分离模型和可选推理环境仍采用外部配置，不作为基础安装包内置内容。程序测试不能替代全部模型、硬件、图形后端或人工听音验证。

最终组合构建、测试、部署冒烟和安装器哈希另由本次发布运行记录给出，不能引用整合前的 163/163 作为新组合通过证据。

## 本次构建与验证

- 最终 Release 全部目标及 `all_qmllint` 构建成功，日志 `build/manual-test-final-build.log`。
- 首轮全量 CTest 为 165/170，通过耗时 822.35 秒。失败目标为缩略图提供器、GPU、音频工具端到端、分离进程和沉浸 QML。
- 修正新增工具 ID 5 后的旧拒绝断言、沉浸面板旧尺寸断言及重复压暗焦点的距离雾；未降低 GPU 图像阈值。缩略图及分离进程单独诊断通过，首次环境相关失败原因未证实。
- 最终对原失败的五个完整 CTest 目标按原设置复验，5/5 通过，111.08 秒；未再次执行完整 170 项，不将复验称为新一轮全量通过。日志 `build/manual-test-rerun.log`。
- 深浅主题的经典、单窗口、滚动、迷你、沉浸和无损鉴别共 12 个界面启动截图通过（退出正常、日志干净）。矩阵 `build/qa/manual-test-final/matrix.csv`。截图不能替代人工交互或准确率验收。
- 已进行整合 Diff Review，并修复滚动主题下无损鉴别定位到错误独立列表的路由。取消的曲库管理页及文件信息面板没有恢复。

安装包为人工测试候选；打包后部署冒烟及安装器 SHA-256 随桌面交付说明提供。
