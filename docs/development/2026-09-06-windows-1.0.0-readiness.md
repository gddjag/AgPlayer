# Windows 正式首版 1.0.0 发布预检

用户要求首次正式编号 1.0.0，后续新发布自动递增。2026-09-07 用户明确授权提交本轮代码、生成 v1.0.0 候选安装包供人工安装与听音，并接受无损鉴别标为“实验性，仅供参考”，后续继续提升准确率。本次候选打包不表示正式验收完成，不上传网站。此前“暂不生成安装包”的门槛仅被此候选授权替代；下列未完成验收仍保留。

## 已完成

- 正式版本单一来源为 `cmake/AgPlayerVersion.cmake`，网站 JSON 模板同步 1.0.0。
- 新发布入口自动计算补丁号；指定同一发布基线可重试而不重复升号。普通构建/重打包不升号，失败回滚版本源。详细命令见版本脚本文档。
- AgPlayer Release 构建通过，实际 PE FileVersion/ProductVersion 均为 1.0.0.0；隔离 QA 曲库下真实 MP3 导入/播放/截图/退出通过，退出码 0。证据：`build/qa/release-1.0.0-version-build.txt`、`release-1.0.0-preflight.png`。
- 版本与打包保护脚本测试 2/2 通过，主代理对最终脚本复跑退出码 0；覆盖自动递增、重复发布基线、两个版本源回滚、错误 PE 拒绝及 CMake 固定 MSVC 工具链。仅构建失败注入，不实际生成安装器。
- 保留上一轮全部 UI 改动及其他工作树内容；未重复合并已包含分支，未提交无关文件。

## 未闭环及准确边界

1. 无损鉴别专业准确率未通过独立验收。1.11/params14 已完成冻结 220 既见集全量回归：有损命中 99/160（61.875%），弃权 121/220，运行错误 0；不是盲测。原生 96k AM 反例 5/5 回归通过，测量仍保留；20 个真实直接升采样样本转为弃权，不计正确，其余 200 个结果与 params7 相同。不能宣传专业认证或将证据分数当准确概率。见 `2026-09-05-lossless-identification-validation.md`。
2. 较新 CUDA 记录已证明约 237 秒整曲、实际 GPU 0、五轨工程输出通过；本轮复用这些真实输出通过应用内波形、混音/solo、seek、音量拖动及主播放互斥测试，默认真实音频后端 3/3 通过，默认 QML 回归 42 通过、1 项 opt-in 跳过。没有重新推理，仍缺人工听音质量验收。见 `2026-09-06-five-stem-application-validation.md`。
3. 当前源码 1561 个 context/source 键的英文缺项已从 584 降至 0，旧兼容键保留；翻译目录、实际 lupdate 源覆盖、翻译管理器检查通过。滚动主题英文截图发现的窄标签已缩短；不将局部截图视作全部页面/DPI视觉验收。见 `2026-09-06-localization-backlog.md`。
4. `AGPLAYER_UPDATE_MANIFEST_URL` 仍为空；用户确认网站在独立 `D:/agwz` 项目由 Cloudflare/GitHub 部署、EXE 放 R2。本轮不改动或部署网站。模板不代表线上服务，正式直连 HTTPS JSON 上线后仍需联调，客户端不存储云凭证。
5. 现有安装器与应用未签名；尚缺干净 Windows 10/11 x64 的新装、升级、关联、卸载验收。本机开发目录启动不能替代该项。签名是否作为首版要求需明确，不能把未签名本身误称为软件无法运行。

旧内测 1.1.x 用户应手动安装正式 1.0.0 一次；正常版本比较不会将 1.0.0 视为 1.1.2 的升级。没有修改比较规则伪造升级。

本轮 AgPlayer Release 重建通过，主代理翻译/安装只读预检 4/4 通过，完整 lossless_analyzer_test 通过。安装预检确认本机有个人数据且无已确认可丢弃虚拟机，因此拒绝作为干净验收环境；未运行安装/卸载。入口和人工清单见 `windows-clean-install-acceptance.md`。

本轮完整 Release 构建通过；全量 CTest 首轮 172/173 通过，唯一失败为安装器静态测试仍要求旧工具链发现方式。修正为校验 CMake 固定工具链后，相关打包测试 2/2 复测通过。重点 Debug 四项（播放体验、地形渲染项、翻译、更新检查）4/4 通过；不等同于全量 Debug 验收。证据见 `build/qa/release-closure-full-ctest.txt`、`release-closure-debug-tests.txt`。

追加标签样式已通过针对性验证：仅名称和数量，未悬停空心、悬停实心，无箭头和投影，所有主题复用公共组件。保留 28px 紧凑高度、原交互和用户保存颜色，新建自动标签超过原 20 色后继续分配未使用 RGB。模型 15/15、标签交互 9/9、截图用例 3/3 通过；深浅主题四张截图已人工查看。证据：`build/qa/tag-color-overflow-green.txt`、`tag-outline-{0,1}-{hollow,hover}.png`，最终 Release 增量构建 `tag-release-final-build.txt` 通过。

正式安装器验收仍未执行。发布前应补足上述验收，或明确批准缩减首版承诺，不能自行隐藏功能或宣称全部完善。未生成正式安装包，未合并未验收实验，未对外上传。
