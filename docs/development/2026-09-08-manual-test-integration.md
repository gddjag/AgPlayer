# v1.0.0 最新人工测试包整合

## 范围

- 用户授权合并全部已完成分支、提交当前代码，包含沉浸视觉，生成桌面 EXE 人工测试；不推送远程、不发布网站。
- 主线共享歌单操作、标签高度/鼠标焦点、分离结果保留与并行配置修复；纳入现有 UI/UX 审查文档，文档建议不代表本轮实施。
- 沉浸视觉增量 `6162c9e`（父 `65440aa`）：内光/顶部细闪、预设参数、控制面板及退出稳定性。作者确认可供人工测试，参考画面质感和跨平台验收仍未完成。
- 版本保持 1.0.0；桌面文件使用日期及提交号区分候选，保留旧安装包。

## 分支与工作树

- 检查全部本地分支，最新沉浸提交为唯一待合入分支，其余提交均已在 main；本地没有远程跟踪分支。
- 其余脏工作树为已有历史整合/对照/回退快照，依据 `2026-09-07-immersive-test-integration.md` 的审计结果保留，不覆盖当前主线。
- `full-ui-performance`、旧 `lossless-identification` 已按块整合；旧 `immersive-visual-lyrics` 和 `lossless-baseline` 为旧实现；task8 已明确回退，task7 为旧 RED 测试。
- 根目录 `'-'` 是既有测试日志，不是源码，不提交或打包，不删除。

## 验收边界

此包用于人工安装、交互、听音及沉浸画面反馈。自动回归与部署启动不替代干净系统安装、签名、真实大型运行时下载、整曲五轨 GPU、跨平台与最终视觉验收。无损鉴别继续标为实验性，仅供参考。

## 集成前验证

- Release全目标构建成功；首轮CTest 178/185通过，用时1043.94秒：`build/qa/20260908-premerge-full-185.log`。
- 两处脚本仍要求28px/11px旧标签尺寸，与最新24px/10px要求冲突；同步明确尺寸断言，保留布局检查。
- 播放变速、无缝队列、设置、分离控制器超时；前三项中的播放/队列及分离按原时限串行通过。设置独立正文45/45、1326ms，随后原CTest 10秒门槛下通过（5.83秒），未调整测试门槛或生产逻辑，未确认系统波动统一根因。
- 版本守护首轮出现vcvars命令行过长；从非嵌套MSVC环境复验通过。六项复验与设置最终结果：`20260908-premerge-recheck.txt`、`20260908-settings-verified.txt`。
- 沉浸静态审查的两项QA隔离P2在`b3712f9`修复：Shell探针限定QA模式、长稳脚本仅清理自有进程。没有新增图形运行时。

## 合并结果验证

- `b776230` 整合后Release全目标构建通过。受影响12项回归10通过，材质失败、翻译超时：`20260908-merged-regression.txt`。GPU整组、主窗口、分离控制器/页面、编辑器、沉浸集成、Windows窗口销毁探针均通过。
- 翻译在原10秒门槛下独立通过（8.54秒）：`20260908-translation-verified.txt`，未调整门槛。
- 材质独立正文15通过/5失败：`20260908-material-diagnostic.txt`。失败为外溢光/半径调节差异和三种材质上下明暗层次。不是环境超时，交回作者最小修复并保留断言，修复前不交付安装包。
- `b4eebb1`仅修正三处着色计算，恢复光照控件与高度梯度，并保持顶侧分界稳定；未改测试断言。主线合并为`bd388bb`，完整Release增量构建成功，主线材质最终20/20通过：`20260908-final-build.txt`、`20260908-material-final.txt`。历史失败记录保留。
