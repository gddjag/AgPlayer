# 2026-09-01 播放器与音频工具统一质量整改

## 范围与边界

- 本记录对应已确认的统一整改规格与实施计划。
- 沉浸视觉第 8 条由专属会话负责，本主线不修改沉浸窗口、地形反应堆或相关着色器。
- 滚动播放主题固定使用频彩波形并移除波形切换入口，已纳入后续滚动主题任务。

## 需求追踪

| 编号 | 需求 | 状态 | 验收证据 |
| --- | --- | --- | --- |
| 1 | 频彩波形频段命名、纯色底色 `#9098A6`、共享进度明暗差 | 完成 | 设置/C++ 波形测试；经典、单窗口、滚动主题 QML 进度与深浅主题测试 |
| 2 | 颜色选择器缩小 | 完成 | 紧凑 HSV 选择器最大 `360 x 430`；确认/取消自动化测试；工作区 Release 实机深色检查 |
| 5 | 波形缩略图优先加载速度 | 完成（复用现有受限加载链） | 可见行优先、离屏取消、双工作线程、256 项缓存/队列边界；provider/item/stress 测试 |
| 10 | 歌曲列表缩略波形亮度，默认 66%，跨主题共享 | 完成 | C++ 持久化/边界/重置测试；共享 `TrackWaveformThumbnail` 实时亮度测试；工作区 Release 深浅主题实机检查 |
| 3/4 | 播放组件固定居中、各主题共享动作顺序 | 完成（经典/单窗口） | 音量展开不改变核心宽度或几何中心；动作顺序 QML 测试 |
| 6 | 文件信息改为竖版小窗口 | 完成（复核既有共享面板） | 共享 `300 x <=470` 面板；歌曲列表和资料库管理两条入口测试 |
| 7 | 歌词浮窗布局与线路状态 | 完成 | 列表底部承载、3 秒隐藏背景并保留歌词、三线路自动切换与失败原因测试 |
| 8 | 沉浸视觉性能与稳定性 | 排除 | 专属会话负责；本主线未修改沉浸代码 |
| 11 | 关于页文案、版本独立显示 | 完成 | `AgPlayer`、动态“版本号”、免费轻便纯净 QML 测试 |
| 9/13 | 双窗口完整波形与筛选高度；滚动主题元数据、固定频彩波形、左侧组件与共享歌曲列表 | 完成 | 滚动主题 14/14；主窗口频彩进度/筛选/主题测试 |
| 14/15 | 音频编辑纵向音量、双端缩放、片段选择、紧凑底栏与导出进度 | 完成 | 音频编辑器 QML 83/83；控制器/视口/波形/原生输入 4/4 |
| 20 | EQ 紧凑原生化 | 完成 | `860 x 520`、18 段、紧凑字体/8px 圆角、自动化测试 |

## Task 1 实施记录：共享波形设置与颜色选择器

完成内容：

- `SettingsController` 新增 `trackWaveformBrightness`，默认 `0.66`，范围 `0.20–1.00`，支持持久化、重置和无效值归一化。
- 歌曲列表设置将“频彩/纯色”与“明亮度 66%”放在同一行，避免设置卡片增高并符合“颜色选项后面加入”的规格。
- 频彩调色板显示八个频段名称，不再显示十六进制色号。
- 原生大颜色对话框替换为项目主题化紧凑 HSV 选择器；取消不写入，确定才提交。
- 纯色波形默认底色回归断言为 `#9098A6`。

验证结果：

- `settings_controller_test`：通过。
- `waveform_item_test`：通过。
- `all_qmllint`：通过；仅保留已有的两个未使用 import 信息提示。
- `MainWindowControls::test_settings_frequency_palette_uses_band_labels_and_compact_picker`：通过。
- `MainWindowControls::test_settings_list_waveform_brightness_is_live_and_defaults_to_66_percent`：通过。
- QML 颜色选择器测试覆盖紧凑尺寸、取消不提交、确认提交。
- 工作区 Release 实机路径：`D:\ai\AgPlayer\build\release\app\AgPlayer.exe`。
- Computer Use 实机检查：深色与浅色下亮度滑条、八频段标签与色块排版正常；紧凑颜色选择器无溢出。

已知基线：

- 完整 `qml_main_window_test` 仍有两个既有失败：真实播放点击寻址期望差异，以及隐藏播放指引时进度色可见性断言；归入后续双窗口/滚动波形任务，不在本 Task 1 冒充通过。
- 视觉验收最初误启动了已安装版本；已通过进程路径核对纠正，并仅以工作区 Release 进程作为本记录证据。

## Task 2 实施记录：缩略波形可见行优先与共享亮度

完成内容：

- 复核并保留现有轻量加载链：只为视口行创建缩略图、离屏立即取消、可见请求优先、最多两个读取工作线程、成功缓存与等待队列均限制为 256 项。
- 将 `trackWaveformBrightness` 直接绑定到共享 `TrackWaveformThumbnail` 的场景图透明度；双窗口、单窗口和滚动主题复用同一 `TrackList`，因此无需各主题重复实现。
- 明亮度变化只更新 GPU 合成透明度，不重新读取缓存、不重新量化峰值，也不重建波形几何。

验证结果：

- `track_waveform_thumbnail_provider_test`：通过。
- `track_waveform_thumbnail_item_test`：通过。
- `track_waveform_thumbnail_stress_test`：通过。
- `MainWindowControls::test_z_thumbnail_brightness_recolors_without_cache_read`：通过，3/3。
- 可见行边界、列表复用和模式切换不重复读取的既有 QML 回归继续通过。

## Task 3 实施记录：播放器动作、文件信息、关于与 EQ

完成内容：

- 经典与单窗口播放器都以传输控制组为几何中心；音量滑条从右侧展开，不再参与核心控制组宽度或反向顶动播放组件。
- 单窗口左侧曲目摘要在窄窗口使用更合理的最大占比，为居中的传输组保留空间；右侧动作顺序继续复用共享组件。
- 关于页产品名、动态版本号和产品承诺分为独立行，版本继续来自构建时 `SettingsController.version`。
- 18 段 EQ 保留 `860 x 520` 可读尺寸，将紧凑模式标题、工具按钮、分段控件、图标和主要面板圆角收敛为接近系统原生的比例。
- 复核既有任务栏入口：Windows 主窗口样式、最小化/恢复/后台激活和窗口组切换已有专门的 `window_controller_test` 覆盖。
- 复核既有文件信息入口：歌曲列表与资料库管理均调用同一个竖版 `AudioFileInfoPanel`，不是空菜单。

验证结果：

- `all_qmllint`：通过；仅保留既有两个未使用 import 信息提示。
- `window_controller_test`：通过。
- EQ、关于、经典/单窗口居中、文件信息两条入口的 7 个聚焦 QML 测试：全部通过。

## Task 4 实施记录：歌词线路与非遮挡面板

完成内容：

- 经典双窗口的歌词面板进入歌曲列表窗口底部布局；开启歌词时自动显示列表窗口，不再把半透明面板压在播放器操作区上。
- 单窗口开启歌词时直接展开已有歌词侧栏；普通歌词面板鼠标离开后 3 秒隐藏玻璃背景、来源、线路提示和操作按钮，只保留歌词正文，移入时恢复操作层。
- 刷新入口改为圆形刷新图标，歌词操作图标统一为 `18 px`，并为提前、延后、刷新、导入补齐悬停提示。
- 保留现有免费三线路自动故障转移：LRCLIB → Unison → lyrics.ovh；逐线路失败会记录提供方名称与用户可读原因，整条链失败后仍可刷新或导入本地 LRC。

验证结果：

- `lyrics_service_test`、`unison_lyrics_provider_test`、`lyrics_ovh_provider_test`、`lyrics_provider_chain_test`：4/4 通过。
- `MainWindowControls::test_list_lyrics_panel_stays_below_track_workspace`：通过。
- `MainWindowControls::test_normal_lyrics_panel_hides_chrome_but_keeps_text`：通过。
- `all_qmllint`：通过；仅保留既有两个未使用 import 信息提示。
- 2026-09-01 本机网络探测：LRCLIB 与 lyrics.ovh 示例请求返回 HTTP 200；Unison 服务可连接，精确示例返回 HTTP 404，确认不是三条线路同时无法建连。

## Task 5 实施记录：共享播放进度与滚动主题

完成内容：

- 经典双窗口和迷你播放器的频彩波形改为“完整未播放底图 + 裁剪已播放叠层”，不再按播放位置反复重算整条频彩波形；深色与浅色主题都获得清晰、连续的进度明暗差，并保留独立播放指引开关。
- 双窗口主波形继续使用精确解码时长与完整共享图层，左端遮罩按深浅主题分别使用黑/白底色，避免首个峰值形成假竖线。
- 滚动播放主题的总览与主波形固定为频彩模式，移除波形切换入口；总览增加已播放裁剪叠层与悬浮时间胶囊，主波形进度随固定中央播放针同步。
- 滚动主题补齐实际格式、位深、采样率、码率、BPM 与文件大小元数据，并复用共享 `TrackList`、`SearchFilter`、`SideNavigation`、`TagManagementPanel` 和歌词组件，后续业务与主题修改不需要复制实现。
- 滚动主题传输组件放在左侧并压缩上下空白；双窗口筛选栏统一为 `42 px` 控件高度，清空键与 BPM 模块同高。

验证结果：

- `all_qmllint`：通过；仅保留既有两个未使用 import 信息提示。
- `tst_rolling_theme.qml`：14/14 通过，覆盖固定频彩模式、隐藏切换键、进度、元数据、悬浮时间、共享列表和最小宽度布局。
- 主窗口频彩波形、紧凑筛选和深浅主题 3 组聚焦 QML 测试：全部通过。
- `git diff --check`：通过，仅有仓库既有的 Windows 行尾转换提示。

## Task 6 实施记录：音频编辑器交互与紧凑布局

完成内容：

- 保留既有纵向轨道音量的自然方向：向上拖动/滚轮向上提高增益，向下拖动/滚轮向下降低增益。
- 将时间线下方的单值横向滚动条替换为双端缩放范围条；初始覆盖完整音频，拖动左右端点直接调整可见起止帧，范围缩短即代表放大波形。
- 在片段头部、音量线、包络点和波形主体补齐 `Ctrl+右键` 选中整片段；选中片段使用半透明主题色与清晰边框，并在快捷键卡片加入对应提示。
- 将底部播放容器从 `104 px` 收敛到 `88 px`，控件保持上下居中；快捷键分隔线增加内边距，快捷键容器自适应填满状态栏上方空间，不再留下无意义底部空白。
- 导出按钮增加绿色从左到右的真实控制器进度、加粗大字百分比和完成态 `✔ 已导出`；失败或结束后退出进度态，不伪造成功。

验证结果：

- `all_qmllint`：通过；仅保留既有两个未使用 import 信息提示。
- `tst_audio_editor.qml`：83/83 通过。
- `editor_viewport_test`、`audio_editor_controller_test`、`audio_editor_waveform_item_test`、`qml_audio_editor_native_input_test`：4/4 通过。
- 聚焦测试覆盖双端缩放、Ctrl+右键片段选择、自然方向音量、紧凑布局、分隔间距与导出进度呈现。
