# 全页面 UI 与性能实施清单

**目标**：逐页检查并整改不统一布局，逐条测量关键性能路径，保留全部功能。沿用已批准的 Theme、暗紫浅蓝配色、紧凑原生桌面结构，不新增视觉框架或重型依赖。

**基线**：主目录 HEAD b1c8a7c 加执行开始时的未提交快照；隔离工作树 full-ui-performance-20260905。其他任务的播放外壳、分离更新合回时三方核对。本轮不提交、安装或打包。

## 页面覆盖与责任

- [x] 主代理：经典 / 整合 / 滚动外壳、迷你、独立列表、搜索、歌词、沉浸、队列、视频和公共控件；逐页审阅常态、紧凑尺寸、深浅主题，并修复实际不一致。
- [x] UI 工具代理：音频编辑及子栏、分离、转换及设置/任务/错误/预检、元数据、文件名处理；保留工具参数和操作，处理表单对齐、文本密度、滚动及窄窗口。
- [x] UI 设置代理：七项设置、均衡器、标签及现有确认交互；统一字体、输入提示、按钮和留白，检查键盘/溢出。已取消的曲库管理与文件信息不属于现行页面，误覆盖改动已撤销，QA 枚举已移除。

## 性能覆盖与验证

- [x] 主代理：播放队列构建、切歌/加载、频谱通知、波形缓存/分析、静止页面重绘、视频；沿现有线程模型验证瓶颈，以可重复输入测量优化前后。
- [x] 性能代理：库加载、导入/元数据、转换；检查重复解析、主线程 I/O、队列/批次复杂度，保留文件完整性和编码质量。
- [x] 分离：与主线任务对齐，检查其线程/资源/取消/输出证据；真实模型由主线验证，本文不把测试夹具等同真实模型运行。

## 交付门槛

- [x] 源码和运行结果对应到保留页面/性能路径；正确部分按检查记录保留。
- [x] 编译、QML 实际实例化、设计系统合同、受影响功能测试与性能基准；首次 163 项中的失败已定位修复，最终资源后相关 28 个完整目标通过。
- [x] 当前 44 张深浅常态截图、紧凑布局复拍和 20 次带实际 DPR 的 125%/150% 捕获通过，细节见结果报告。
- [x] 独立审查及主代理最终 Diff Review，冻结基于快照的代码增量；不包含主线已删页面，也不覆盖频彩已整合的 provider。
- [x] 报告保留项、已优化项、实测收益和平台/硬件限制；Windows 证据不能替代 macOS/Linux。
- [x] 主线按 hunk 合入冻结代码增量并完成组合回归：完整 Release / all_qmllint / 163 个 CTest 均通过，九类页面深浅 18 张截图通过。

## 追加体积优化

- [x] 区分构建目录、运行部署与压缩安装器，核对 PE 依赖、动态 Qt 插件、资源白名单和重复文件。
- [x] 正式发布脚本精简 16 份 `.qmltypes`，实际减少 1,164,172 字节；临时目录行为测试和安装器合同通过。
- [x] 取消 20 个未使用资源的嵌入；受控相同源码构建净减少 EXE 103,424 字节。主线删除 19 个无调用 SVG 源，仅保留仍作为磁盘测试 fixture 的品牌图，运行功能保留。
- [x] 精简后的独立运行目录净 PATH 下四页启动/退出通过；真实格式导出读取、原生文件/颜色与转换对话框检查通过，未借用 SDK/vcpkg。
