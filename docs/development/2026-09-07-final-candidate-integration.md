# v1.0.0 最终人工验收候选整合

## 范围

- 主线人工反馈修复提交：`7a54e8b`，提交前 Release CTest 173/173 通过，日志 `build/qa/preintegration-full-tests.txt`。
- 整合 `codex/lossless-accuracy-round3` 的 `8f4d90c`：1.13 / params16。保留主线 AM 中性证据、空处理链和严格反例测试；保留“专业模式”翻译及“实验性，仅供参考”标识。
- 新的 MPEG 分析窗来源说明及许可证随安装包放入 `licenses/`；不增加大型模型或推理运行时。
- 当前产品版本仍为 1.0.0，本包用于最终人工安装、交互和听音验收，不是已完成正式上架验收的声明。

## 分支排除项

- `codex/immersive-gel-material-20260907`：作者明确未完成；GPU 30 通过/1 失败，完整视觉与长期稳定性未通过。保留工作树，不整合。本包继续使用主线原生 QRhi 渲染器。
- 其他已登记分支提交均已在 main（整合前仅 lossless-accuracy-round3 未合入）。旧 task8 门禁/红测、task7 红测、lossless 对照基线不是发布成果。
- full-ui-performance、旧 immersive-visual-lyrics、旧 lossless-identification 的未提交内容已有历史按块整合记录或属于较旧实现；不得整目录覆盖当前主线。全部原样保留。
- 无网站部署、远程推送或源码工作树删除；不纳入根目录原有未跟踪 `'-'` 文件。

## 人工重点

1. 新配置主波形纯色、缩略图频彩；升级保留已有选择。
2. 专业模式命名、三行信息、波形高度、校准浮窗拖动与主题菜单选中配色。
3. 音量百分比紧邻滑条、鼠标点击无紫色焦点边框、迷你外圈无描边。
4. 主播放、编辑器及分离试听任意切换只保留一个播放源，位置可恢复。
5. 实际模型首次校验、重复切换、校验中一键配置、暂停/取消/重试。
6. 无损鉴别仅为信号证据推断；专业识别准确率和稳定整曲性能仍未完整验收。

Windows 签名、干净系统安装/卸载、人工听音及公开分发许可完整审查不因构建或自动测试通过而自动完成；发布前应核对这些事项。

## 对外发布保留项

- 独立代码审查未发现此次 lossless C++/Qt 生命周期、取消或异常处理的新阻断。
- MPEG 分析窗包含来自 FFmpeg 的 LGPL 系数表并编译进入程序；附带 NOTICE 和许可证不代表公开分发安排已完成。正式上架前仍需确认相应源码/可重链接材料等获取安排。本包仅交付本机人工验收，不作为对外发布授权或合规结论。

## 本轮验证

- Release 整合构建成功；全量 CTest 首轮 180/181，通过项包含新增 MP3/CELT 探针、主窗口/迷你/沉浸集成与播放相关测试。唯一失败为打包测试临时夹具漏带新增许可证；补全夹具后 installer_contract_test 与 package_version_guard_test 2/2 通过，未修改产品断言。
- Debug MP3 hybrid 测试通过；综合 analyzer 在原 300 秒预算下超时。独立诊断自然完成，退出 0，用时 465.5 秒，CPU 持续运算且工作集稳定。将该综合功能测试的 Debug 预算设为 900 秒，Release 仍为 300 秒；不改变单文件性能要求、分析阈值或断言。
- 调整预算后 Debug analyzer 的 CTest 定向复测通过；日志 `build/qa/final-debug-analyzer-recheck.txt`。没有运行完整 Debug 测试集。
- 部署目录 EXE 在隔离 QA 配置中启动、播放 sine-440hz.wav 并正常退出（0）；截图 `build/qa/final-candidate-main.png` 确认为经典双窗口及纯色主波形。此项不是人工听音或干净系统安装验收。
- 安装包版本 1.0.0.0，35,920,012 字节，未签名。SHA-256：`E1FA5411D0B4E149510377A7E0BD096CF35494FB7D03EA145E2EB056E9E35426`。
