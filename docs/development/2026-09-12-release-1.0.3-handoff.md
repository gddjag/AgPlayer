# 1.0.3 发布交接给“版本发布”专项任务

## 授权与当前状态

- 用户已确认最新人工测试版，并授权“发布为 1.0.3，删除线上仓库和 R2 的旧版本文件”，随后明确要求交给“版本发布”专项任务执行。
- 主线已停止线上操作。本会话没有跨任务发送/移交工具，因此本文是待接收的交接单，不代表已通知或已发布。
- 截至本次交接：未创建 GitHub 1.0.3 Release、未上传文件、未更新线上清单/网站、未删除任何线上文件。
- 本地仓库 `D:\ai\AgPlayer`，分支 `main`，前一正式版提交为 `f9da615c`。用户随后要求提交新版本代码：本交接单所在的 1.0.3 主线提交包含全部已验收功能、相关测试与版本修改，发布分支应同步该完整提交后备份；可用 `git log -1 --format=fuller` 与 `git show --stat HEAD` 核对。本地播放器仓库没有 remote，不要把播放器源码上传到公开下载仓库。

## 已完成的本地包

- 命令：`scripts/package-windows.ps1 -BuildDirectory build/release -Configuration Release -NewRelease -FromVersion 1.0.2`。
- 构建/安装器生成成功；版本源与 `deployment/updates/latest.json` 已更新到 `1.0.3`；主程序与安装器 PE 版本均为 `1.0.3.0`。
- 安装包：`D:\ai\AgPlayer\build\installer\AgPlayer-Setup-1.0.3-x64.exe`。
- 大小：36,203,853 字节；SHA-256：`44247C0FFA169E19AA6B5E23D62D47F1B57AFFBC95AE738C0E184E81C8D28BED`。
- 桌面与 E 盘副本均已复制并比对 SHA-256：`C:\Users\Administrator\Desktop\AgPlayer-Setup-1.0.3-x64.exe`、`E:\AgPlayer 备份\AgPlayer-Setup-1.0.3-x64.exe`。
- 日志：`D:\ai\AgPlayer\build\qa\release-1.0.3-package.log`；差异检查通过。
- 未签名，沿用用户选择的无证书发布方案。
- 版本提升后尚未执行独立的打包启动冒烟和版本/在线更新验收，请接手任务完成这些局部检查，无需重新做已确认的全项目人工测试。

## 此包包含的已验收功能

- 五个颜色色块独立为用户“自定义颜色”，即时预览、确认保存、取消恢复；不修改固定预设。
- 修复选色弹窗打开超过 5 秒后，父控制台自动隐藏导致操作卡住的问题。
- 删除“黎明青柠”，新增并按用户二次确认校正“紫夜霓心”：紫色主体与冲击波，中央顶起内光粉红、中心略带暖黄；其他预设和音频节奏算法保持不变。
- 同一功能代码在 1.0.2 测试包下已通过编译、配色/渲染绑定测试、GPU 像素与静音/顶起检查、自定义选色回归及参考冲击波/亮片渲染回归。用户确认的测试包为 `AgPlayer-Setup-1.0.2-人工测试-20260912-223956-x64.exe`。
- 相关证据：`build/qa/violet-gpu.txt`、`build/qa/violet-reference-material.txt`、`build/release/tests/qml-immersive-custom-colors.txt`、`build/qa/custom-violet-heart.png`。

## 已只读核对的线上信息（执行前再次刷新）

- GitHub 公开网站/下载仓库：`gddjag/AgPlayer`，默认分支 `main`；本机 Git Credential Manager 现有凭据有 push 权限。没有安装 `gh`，可沿用 GitHub REST API；不要打印或落盘凭据。
- Releases 接口当前仅返回 `v1.0.2`：Release ID `387578268`；EXE asset ID `559303908`，文件名 `AgPlayer-Setup-1.0.2-x64.exe`，36,202,989 字节。
- 现有 Actions：`Sync release EXE to R2`，workflow ID `351638779`，路径 `.github/workflows/sync-release-r2.yml`。应先读取当前 workflow，复用既有 R2 同步；不要另建下载基础设施。
- 官网：`https://www.agplayer.com/download`；国内更新清单：`https://download.agplayer.com/updates/latest.json`。
- 当前旧包：`https://download.agplayer.com/releases/v1.0.2/AgPlayer-Setup-1.0.2-x64.exe`。
- 先前发布记录：`D:\ai\AgPlayer\docs\development\2026-09-12-release-1.0.2.md`；其中历史状态仅供参考，当前 GitHub 旧版本列表与该旧记录不同。
- `D:\agwz` 目前不是 Git 仓库，`build/qa/release-1.0.2/site` 也只是此前验收用的少量网站文件，不是完整发布工作树。应以线上最新网站源码为准，仅更新版本/下载/校验值，不覆盖网站设计；主页的百度统计脚本必须保留。

## 后续执行顺序

1. 同步本交接单所在的完整 1.0.3 主线提交并备份；校验上述安装包与二进制版本，完成打包启动、版本契约、在线更新相关检查。
2. 发布 GitHub `v1.0.3`，附件严格使用 `AgPlayer-Setup-1.0.3-x64.exe`；按现有 workflow 同步 R2。
3. 更新官网备用下载版本/链接/SHA-256、必要的缓存版本，以及在线更新 JSON；保留原有“检查新版 → 官网下载 → 用户安装”机制，不声称支持静默自动安装。
4. 验证 GitHub/R2 新安装包下载哈希、官网链接、更新接口（1.0.2 提示 1.0.3，1.0.3 显示已是最新版）。
5. 新版可用后，精确盘点并删除被替代的旧安装包及其版本目录内校验文件；不要删除仓库、网站资源、整个 R2 桶、当前版本或更新入口。用户授权的是旧版本文件，默认保留历史提交/标签。
6. 复查新版仍可下载，旧地址下线。记录准确删除清单、可恢复备份及线上结果，通知用户。

## 删除前可用的旧版恢复文件

- E 盘根目录下先前的正式 1.0.2 安装包当前不存在，不要照旧记录假定它还在。
- 本机已核对存在的 1.0.2 正式原包：`D:\ai\AgPlayer\build\qa\release-1.0.2\r2-public.exe`。
- 它的 SHA-256 为 `AF6839CFAC746E66DFF61091FB53C73B5F730E1FB04564A34A5855C902E676CB`，与原正式包一致。线上删除前可将它按版本名归档到 E 盘，保留恢复能力。
