# Windows 1.0.0 发布检查（未放行）

本轮基线为 `main@877b58d`，包含此前尚未提交的浅色配色及中文提示修复。没有生成或上传正式安装包。根目录 `'-'` 历史日志保留，不纳入代码或安装包。

## 在线更新

- 首版版本源仍为 `cmake/AgPlayerVersion.cmake` 的 `1.0.0`。
- 客户端默认检查 `https://download.agplayer.com/updates/latest.json`，读取 `schemaVersion: 1` 和 `version`；HTTPS、大小上限、版本解析和总超时保护保留。
- 关于页始终提供官网入口。检查失败提示切换 GitHub / R2；明确浏览器负责安装包下载，客户端不伪装检测浏览器下载状态。
- 网站部署任务已在 `D:/agwz-deploy` 准备同步脚本：稳定 Release 的 EXE 完整同步后最后发布版本 JSON，双线路同版本同哈希，旧版本不得覆盖新指针。主线独立运行 `node --test scripts/sync-release-r2.test.mjs`，7/7 本地测试通过；未执行远端上传，不能据此声称线上已接通。
- 实测官网首页可访问；官网 `/updates/latest.json` 返回首页 HTML，R2 更新 JSON 尚未上线。不能报告在线更新联调完成。

## 发布问题跟踪

1. 下载保护已修复：`.part` 拒绝链接/重解析点，打开前后检查，网络缓存限制 64 KiB，超过清单大小的响应停止写入；独立 installer 完整测试 24/24，包含 256 KiB HTTP/file 响应完整读取。主线最终回归中的 installer 已通过。
2. 文件操作已改为备份/暂存/提交；数据库路径更新失败时回滚，恢复不删除其他程序创建的目标，极端失败保留并报告恢复路径。独立文件操作与曲库测试 2/2；主线最终回归待执行。
3. 外置 Python 改为精确 marker 契约；一键配置异步验证已有环境，旧 marker 也先验证，失败通过已校验 uv 一次修复并重新安装依赖，保留 env 和缓存。5 项定向测试通过；未真实下载/导入端到端验收。传递 wheel 依赖仍未全量哈希锁定，不宣称完全可复现的离线环境。
4. 经用户确认，SoundTouch 已改为独立 DLL；构建拒绝 Windows 静态目标，实际 EXE 导入表已确认 `SoundTouch.dll`。已补主要依赖许可文本、Qt 6.7 部署模块第三方归属和打包复制/缺失检查，共 44 个文件约 265 KiB。对应源码的对外提供方式仍需落实；不能仅增加 notice 即宣称合规。
5. 沉浸分支修正到 `6086e31` 后已进入主线合并暂存区：保留预设 52，声明首次场景焦点（原键盘失效在 offscreen 未复现，属于预防修正）。合入后主线 6/6 相关测试通过，包含 GPU 冒烟和 QML 集成。
6. 当前安装包未签名；这是发行信任风险，不将其单独认定为法律强制阻断。没有干净 Windows 安装/升级/卸载及当前包实际听音的现行证据。

## 已执行验证及边界

- Release 全目标构建通过。
- 最终全量回归执行完 186 项，183 项通过：控制器旧 worker 升级夹具失败，播放控制 30 秒超时、翻译扫描 10 秒超时。保留失败原日志 `build/qa/20260908-final-release-tests.txt`，不覆盖为通过。
- 外置环境升级保留安全解释器并同步配套脚本；完整控制器独立复跑 90 通过、2 项跳过。既有环境修复跳过 venv 创建，执行依赖重新安装；主线原配置 CTest 已通过（66.97 秒）。
- 翻译扫描另有逐条遍历整表的二次复杂度；改为一次 Ordinal 索引，保留重复项最后生效及所有漏译/占位符断言，同机直接运行 5.44 秒降至 1.19 秒，原限额 CTest 2.30 秒通过。
- 索引独立审查在真实中英文 catalog 的 3,859 个键上与旧查找命中完全一致，重复 context/source 的最后生效规则保持。
- 播放控制直接运行 52/52 通过（18.83 秒）；外部控制器测试并发时同样通过但墙钟约 30.22 秒。暂停其他测试后，原 CTest 独占通过（27.14 秒）。没有改超时或断言，现进行无外部并发的全量回归。
- 无外部构建/测试并发的整轮最终为 185/186 通过（839.55 秒），仅播放整组总限额再次超时，日志 `build/qa/20260908-release-isolated-final.txt`。随后完整直接运行仍 52/52 通过（17.42 秒），未发现单项挂死。
- 因此修正测试预算而非播放代码：52 项集成测试整组 90 秒，每个测试函数使用 QtTest 30 秒看门狗，保留 RUN_SERIAL 和全部行为断言。新配置真实 CTest 1/1 通过（23.04 秒），证据 `build/qa/20260908-playback-controller-timeout-policy.txt`。没有在该预算修改后重跑整套，不报告一轮 186/186 全绿。
- Release 首轮 186 项中 183 通过：播放控制超时、翻译扫描超时、主窗口测试失败。证据 `build/qa/20260908-release-all-tests.txt`。
- 播放控制原时限串行复跑通过（19.10 秒）；翻译扫描排除 CMake 混入的 Qt SDK 和构建生成文件，保留实际项目源和漏译检查，未放宽断言或超时；独立审查者优化后原限额运行通过（8.13 秒）。
- 更新及相关回归 7 项中 6 项通过，主窗口 CTest 仍失败。独立有日志、指定 Windows 字体的主窗口运行 146 通过、0 失败、1 项平台跳过，退出 0；不能据此抹去前面的失败或声称全量绿色。
- Debug 更新及翻译 2/2 通过。
- Debug 文件操作、下载器和更新检查 3/3 通过（30.64 秒）。其后下载器又增加大文件循环读取修复，需最终 Release 验证覆盖该增量。
- SoundTouch DLL 改造后 Release 全目标构建通过；变速/音高/EQ/更新相关 6 项通过，补齐许可证测试夹具后打包版本保护测试单独通过。
- 主窗口失败最终定位为 SettingsPage 的 SettingCard 外层布局/高度反馈循环；改用按内容高度排列的 Column 后，原失败 3 项通过，完整 CTest 主窗口测试通过（59.05 秒）。浅色关于页已实际启动截图，中文、颜色、布局和官网备用入口可见。
- 本轮更新前的六个音频工具浅色页面截图已检查，14 项相关 UI/翻译回归通过；不等于所有窗口、DPI、交互状态均已验收。
- 审查覆盖当前 UI/翻译改动、下载/外置运行时、文件操作、播放/数据/QRhi 关键路径、版本/打包脚本。独立审查结果已核对关键代码；不声称逐行证明整个项目无缺陷。

正式包需先关闭上述实际缺陷及上线依赖，再重新构建、回归和进行部署版验收。此记录不是发布许可证法律意见。
