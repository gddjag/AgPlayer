# Windows 1.0.0 发布缺口补齐计划

**Goal:** 补齐本机可执行的发布验证和英文完整性，保留无法由代码替代的听音、独立准确率、签名与线上部署门槛。

**Architecture:** 复用现有 Qt/QML、原生分离与安装流程；各领域独立工作，构建由主代理串行调度。保留其他工作树实验，不整体搬运未验收算法。

**Tech Stack:** C++17、Qt 6.7、PowerShell、Inno Setup、ONNX CUDA。

**Spec:** `2026-09-06-windows-1.0.0-readiness.md` 与用户“现在开始完善补齐”。

## 执行与验收

- [ ] 英文：临时 lupdate 提取当前上下文，匹配 `context + source` 补齐 `translations/agplayer_en.ts`，保留旧兼容项；新增源覆盖检查，先失败再通过。运行 `translation_catalog_test`、`translation_manager_test`、`phase6_translation_coverage_test` 及新覆盖测试，核对关键英文窗口截图。
- [ ] 五轨：使用已有实际 CUDA 输出和对应源 MP3，走控制器结果处理和真实波形加载路径；扩展 `qml_audio_tools_test` 的 opt-in 真实结果验收，覆盖波形、混音播放/暂停/seek、音量拖动与主播放互斥。测试不得注入假峰值。应用流程、实际音频设备与人耳听感分别记录。
- [ ] 鉴别：使用主线 params-7 对已冻结 c01 的 220 文件重新评估，保留旧 params-6 记录；明确为既见集回归而非新盲测。按编码族报告召回与明确控制误报，弃权不算正确；只修复有可复现根因的缺陷，不调低门槛或混用未集成实验结果。
- [ ] 安装：检查干净 Windows 可用性；不得在已有用户安装上运行会改写同一 AppId 的试验安装。若无隔离环境，准备可重放的新装/升级/卸载验证入口，并明确未执行，不将开发目录启动当作安装验收。
- [ ] 更新与签名：JSON 地址尚未部署，网站另在 `D:/agwz`、Cloudflare/GitHub/R2 工作流中，不改动该项目；播放器继续使用公开 HTTPS 版本清单和官网跳转，不持有云凭证。签名等待有效证书或服务，不使用自签名冒充可信签名。
- [ ] 集成：相关目标构建、针对性回归、最终审查与截图，条件具备后才运行正式发布前全量检查。未满足门槛不生成正式包。

所有新代码保留失败→通过证据；翻译和人工说明无需虚构行为测试。新增错误或依赖缺口记录到同轮验收文档，不伪装成全部完成。

## 本轮阶段结果

- 英文当前源覆盖缺失 584→0；源覆盖/目录/管理器通过，滚动主题英文截图复核并收紧了四处窄标签。全部页面与 DPI 验收未完成。
- 真实保留 CUDA 五轨输出的应用流程 3/3 通过；默认分离 QML 42 通过、1 opt-in 跳过；人工听音仍待完成。
- params7 完整 220 既见集回归完成；新 params14 AM 误报修复及 20 升采样代价回归完成。专业级独立准确率仍未通过，不使用弃权抬高成绩。
- 安装只读预检及安全测试完成，不在现用安装上实验；实际干净 Windows 安装/升级/卸载尚未执行。
- Release 应用重建通过，PE 1.0.0.0。未生成正式安装包或上传网站；更新线上联调和签名选择仍待外部条件。
