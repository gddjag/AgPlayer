# AgPlayer 原生人声伴奏分离验收记录

日期：2026-08-30

分支：`codex/vocal-separation-native`

平台：Windows x64、Qt 6.7、VS2022 Release、ONNX Runtime DirectML 1.24.4

## 结论

功能实现、真实三档模型 CPU 推理、DirectML 完整长音频推理、主进程隔离、便携包轻量化和 Release 回归已经通过。用户最后一轮要求的紧凑工作台界面已在四档尺寸完成截图复核。正式安装包仍不生成：参考图是“模型已安装并完成分离”的结果态，当前实机对照是“真实音频已载入、模型未安装”的可用初始态，状态不一致，不能据此宣称完成结果态完全一致；真实声卡试听和人工点击全流程也仍需最终人工验收。

## 必须项映射

| 必须项 | 实现与证据 | 状态 |
|---|---|---|
| 第二个可见标签且内部 ID 稳定 | 导航顺序 `[0, 4, 1, 2, 3]`；布局契约测试覆盖 | 通过 |
| 主进程不链接 ORT | 空闲 AgPlayer 进程模块检查为 0 个 `onnxruntime*`；Worker 为独立进程 | 通过 |
| 三档模型与固定官方下载 | Catalog 固定 URL、字节数、SHA-256、来源和轨道；下载器覆盖 Range、续传、错误哈希和原子激活 | 通过 |
| CPU 与 DirectML | 三档 CPU 黄金测试通过；KARA DirectML 短音频及 5/10 分钟完整输出通过 | 通过 |
| 两个真实适配器 | Worker 只实现 MDX 与 Demucs；Demucs 伴奏由鼓、贝斯、其他合成并做削波保护 | 通过 |
| 失败、取消与事务输出 | 协议、超时、崩溃、幂等取消、路径安全、临时目录恢复和原子目录提交测试覆盖 | 通过 |
| 共享预览、波形、历史 | 单一预览控制器、共享绝对位置、有限峰值、`QSaveFile` 历史和 500 条上限测试覆盖 | 通过 |
| 响应式 UI | 1672×941、1280×720、880×560、1920×1080 自动布局测试和截图覆盖 | 通过 |
| 100%–200% DPI | 真实载入 3 分钟音频的 100%/125%/150%/200% 截图均无裁切、重叠或主操作丢失 | 通过 |
| 包体积增量 | 最新 Release 二进制：主 EXE +589,312 字节、Worker +475,136 字节，总增量 1,064,448 字节 | 通过 2 MiB 硬上限；比 1 MiB 目标多 15,872 字节 |
| 未使用时零常驻 | 未进入工具时 Worker 数 0、ORT 模块数 0；退出后 Worker 数仍为 0 | 通过 |
| 正式安装包门禁 | 未生成安装包；保留视觉结果态、人工音频硬件与最终点击验收门禁 | 按计划阻止 |

## 真实模型与音频结果

短音频黄金测试使用固定 44.1kHz 立体声 WAV，并通过生产 `Decoder` 重新打开每条输出，验证轨道数、长度、采样率、声道、非静音、有限值和削波。

| 模型 / 设备 | 输出 | 重构归一化 RMS | 结果 |
|---|---:|---:|---|
| UVR MDX KARA / CPU | 2 轨 | `2.58737e-06` | 通过 |
| UVR MDX Inst HQ3 / CPU | 2 轨 | `2.38544e-06` | 通过；最终测试代码再次运行退出 0 |
| HTDemucs FT FP16 / CPU | 5 轨 | `0.0197768` | 通过；最终测试代码再次运行退出 0 |
| UVR MDX KARA / DirectML | 2 轨 | 短音频完整结果 | 通过，provider 为 `directml` |

公开领域验收音频来自 [Wikimedia Commons: Je te veux](https://commons.wikimedia.org/wiki/File:Je_te_Veux.ogg)，源文件 SHA-256 为 `7B0B1C89E23D678779AF59E7FAC2B06AAF143D12BF2B9F36D2CC7DB7C833D98A`。从该文件生成 44.1kHz 立体声 FLAC 压力样本：

| 时长 | 设备 | 实测耗时 | 验证 |
|---:|---|---:|---|
| 180.000 秒 | CPU | 132,252 ms | 2 轨、长度/格式/有限值/非静音/削波通过 |
| 299.988 秒 | DirectML | 14,262 ms | 2 轨完整输出通过，无 CPU 回退 |
| 599.977 秒 | DirectML | 31,203 ms | 2 轨完整输出通过，无 CPU 回退 |

对应日志位于 `build/qa/vocal-separation-final/`：`golden-kara-stream-refactor.txt`、`golden-hq3.txt`、`golden-demucs.txt`、`directml-kara.txt`、`duration-3min-kara-cpu.txt`、`duration-5min-kara-directml.txt`、`duration-10min-kara-directml.txt`。

## 体积与隔离

以下是 2026-08-30 在同一台机器、同一 VS/Qt/vcpkg 配置分别构建基线提交 `4a2f034` 和当日功能提交，并使用同一 `tools/stage_release.ps1` 分发规则得到的历史比较：

| 产物 | 基线字节 | 功能字节 | 增量 |
|---|---:|---:|---:|
| `AgPlayer.exe` | 5,624,832 | 6,164,992 | 540,160 |
| `AgSeparationWorker.exe` | 0 | 464,384 | 464,384 |
| 完整便携目录 | 122,821,084 | 123,825,628 | 1,004,544（981 KiB） |

便携目录共 513 个文件，不含 ORT DLL，不含 `.onnx` 模型。外置 `onnxruntime.dll` 为 17,328,152 字节；KARA 为 29,704,436 字节；HQ3 为 66,759,214 字节；Demucs 四文件各 165,612,636 字节。

开发要求给出的旧便携基线为 117,005,514 字节。当日同环境重建旧提交得到 122,821,084 字节，说明工具链/部署内容已有 5,815,570 字节漂移；因此旧基线与当前包的绝对差值不能当作功能增量。

Windows 发布链已补齐两处真实缺口：`scripts/package-windows.ps1` 和 `tools/stage_release.ps1` 都会复制并验证 Worker；后者的默认路径也改为在参数解析后解析，避免 Windows PowerShell 中 `$PSScriptRoot` 在参数默认值阶段为空。

## UI 对照证据

以下对照图均为左侧参考、右侧实机，实机载入真实 3 分钟音频，不使用硬编码歌曲、模型状态、历史或完成标志：

- `build/qa/vocal-separation-final/comparison-1672x941-loaded-dpi100-v2.png`
- `build/qa/vocal-separation-final/comparison-1672x941-loaded-dpi125-v2.png`
- `build/qa/vocal-separation-final/comparison-1672x941-loaded-dpi150-v2.png`
- `build/qa/vocal-separation-final/comparison-1672x941-loaded-dpi200-v2.png`
- `build/qa/vocal-separation-final/comparison-1280x720-v2.png`
- `build/qa/vocal-separation-final/comparison-880x560-v2.png`
- `build/qa/vocal-separation-final/comparison-1920x1080-v2.png`

布局、层级、局部深蓝主题和响应式行为已实现；但参考图的已安装模型、下载进度、五轨结果和历史属于不同真实状态。未通过生产代码注入假状态，因此最终同状态视觉验收仍为阻塞项。

### 2026-08-30 UI 调整复核

根据用户对首版页面的纠偏，完成了以下最小范围调整：

- 输入/歌曲预览压缩为 150px 高，波形前增加播放键，播放位置、点击定位与底部运输控件共享同一预览控制器。
- 输入波形及五条结果波形全部复用播放器 `WaveformItem` 和现有波形设置；结果区提高到 286px，不保留页面私有波形绘制。
- 模型区压缩为 196px，改为横向 `ListView` 与常显滚动条；模型卡显示档位、徽标、模型名称、提供商、模型介绍、官方仓库、文件大小和真实状态，并保留安全 manifest 自定义模型入口。
- 五个输出音轨均有项目内图标、用途说明、勾选状态和明确的“不支持”状态；两轨模型不会生成虚假多轨结果。
- 底栏补齐归零、播放/暂停、共享时间、重新分离、导出、人声/伴奏/所选轨、加入播放列表和开始分离。
- 输出设置使用局部深色下拉/复选控件，连接真实格式、目录、设备探测、完成后加入播放列表及自动打开目录行为；历史表连接真实记录、重新使用输入与打开目录动作。
- 新截图证据位于 `build/qa/vocal-separation-rework/`：`1672x941-final-v2.png`、`1280x720-final-v2.png`、`880x560-final-v2.png`、`1920x1080-final-v2.png` 和 `comparison-1672x941-final-v2.png`。四档截图日志均为空，无 QML 运行时警告。
- 独立只读审查发现的历史目录错指、`file:///` 特殊字符误解析和 1280 历史列挤压均已修复：记录操作传入本行 `outputPath`，历史输入由 C++ `QUrl::fromLocalFile` 转换，1100–1439px 历史行改为“文件/模型 + 时间/状态”堆叠。官方仓库链接也补齐键盘焦点和 `Accessible.Button`。

聚焦验收测试 `vocal_separation_install_test`、`vocal_separation_history_test`、`vocal_separation_controller_test`、`qml_vocal_separation_test`、`audio_tools_layout_contract_test` 为 5/5 通过；审查修复新增了特殊字符/Unicode 历史输入和 1280 真实历史记录布局回归测试。参考图仍是已完成任务状态，本轮截图是真实短 WAV + 未安装模型状态，故最终同状态视觉验收继续保持阻塞，不以假历史或假波形绕过门禁。

### 2026-08-30 最终紧凑工作台复核

- 输入/歌曲预览进一步压缩为 116px 高；取消封面并加长统一 `WaveformItem`，保留歌名、文件大小、时长、播放进度及点击定位。输入播放只驱动输入波形，不再联动下方结果波形。
- 空格键在当前分离页面默认切换结果试听的播放/暂停；结果生成后由底部统一运输控件播放全部可用结果轨，点击单条波形则从点击位置独奏该轨，不为五条轨道创建独立播放键或常驻播放器。
- 模型区压缩为 174px，点击整张卡片即选择并高亮，保留“极速轻量 / KTV 纯净伴奏 / 标准音质 / 自定义模型”与“低配优先 / 推荐”等徽标。横向滚动条移至卡片边框下方，支持将来的安全 manifest 自定义模型横向扩展。
- 五个输出音轨采用项目图标和有许可证的 Lucide 图标，显示说明及常显勾选框；选项不占满整行，右侧提供“备用模型下载地址”。当前没有可信网盘地址，弹窗明确显示暂无地址并支持复制说明，不使用虚假链接。
- 结果区提高到 350px；每轨音量条位于统一波形之前，无单轨播放键。右侧设置支持 WAV/FLAC/MP3 与自动/CPU/GPU，历史仍连接真实控制器数据。
- 底栏只保留播放控件/时间、重新分离、导出伴奏、导出人声、导出所有音轨和开始分离。新增 `exportAll` 始终导出所有可用结果轨，不受当前勾选状态影响。
- 最终实机图位于 `build/qa/vocal-separation-final/`：`1672x941.png`、`1280x720.png`、`880x560.png`、`1920x1080.png`；同图对照为 `comparison-1672x941-refined.png`。主尺寸复核通过；低高度按设计使用滚动内容并保持主操作可见。
- 新增回归覆盖整卡选择、空格播放、输入/结果播放位置隔离、音量控件顺序、底栏动作集合、MP3 输出以及“导出所有可用轨”。

本轮用户指定的界面整改状态：通过。真实模型完成结果态、DirectML 实机交互和声卡试听仍属于正式发布门禁，不因本轮 UI 通过而自动关闭。

### 2026-08-31 播放、设备、命名与进度条收口

- 上方输入试听和下方结果试听继续复用一个轻量预览控制器，但各自保存播放位置；输入使用橙黄色播放键，结果使用绿色播放键，空格只切换结果试听。来回切换不会把另一套位置清零，结果音轨音量也不会泄漏到输入试听。
- 模型横向卡片继续支持滚动；下载/暂停/删除控件保持紧凑。自定义卡保留参考图的独立立方体样式，但删除了未实现的“自动识别”承诺，明确当前仅执行 catalog 中的受信模型。
- 五轨音量控件支持点击、按住拖动、鼠标滚轮和键盘调整；五条结果波形共用一个结果播放头和一个底部运输控件。底部播放为全部可用结果轨的同步混音，点击单条波形切换为该轨独奏并按点击位置定位。
- 空进度条改为低透明度中性灰描边；进度填充和右侧百分比保持绿色。
- “完成后打开目录”选项保留且默认不勾选；用户主动勾选后，任务完成仍调用真实打开目录行为。“导出后加入播放列表”保持独立。
- GPU 探测只把 DXGI 结果描述为 DirectML 候选；正式开始任务时使用受信模型做最小推理验证。Worker 会依次尝试全部硬件适配器，GPU 模式全部失败时返回完整诊断，Auto 才回退 CPU。
- Start 消息新增命名字段后协议升级为 v2，v1/v3 被明确拒绝，避免新旧 GUI/Worker 通过握手后才出现字段错误。
- 输出文件名为 `原歌曲名-本地化音轨名-catalog 模型显示名.格式`；音轨名在点击“开始分离”时按当前软件语言快照。输出目录仍为 `原歌曲名-modelId`，冲突时自动编号，不覆盖已有结果。
- 最终视觉证据为 `build/qa/separation-reference-vs-implementation.png`、两张聚焦对比和四档响应式截图；详细判断写入项目根目录 `design-qa.md`。
- 最终 VS2022 x64 Release 构建通过；完整 CTest 为 89/89 通过（340.11 秒）。新增/更新测试覆盖试听位置往返、输入音量恢复、语言快照、键盘音量、默认未勾选自动打开目录、多 GPU 适配器遍历、协议 v2、错误版本夹具和同步结果混音。
- 最新 `AgPlayer.exe` 为 6,214,144 字节，相对同环境基线增加 589,312 字节；Worker 为 475,136 字节，总二进制增量 1,064,448 字节（约 1.015 MiB）。这低于 2 MiB 硬上限，但比 1 MiB 目标多 15,872 字节；本轮没有为体积数字生成或宣称正式安装包。

## 自动回归

- VS2022 x64 Release 全量构建：通过。
- CTest：本轮最终重新执行 `ctest --test-dir build/msvc-release -C Release --output-on-failure`，结果为 `100% tests passed, 0 tests failed out of 89`，总计 340.11 秒。
- 分离相关测试覆盖 Catalog、下载、运行时、协议、Worker、事务输出、Controller、历史、QML、布局与发布契约。
- 全量回归中发现并修正两个旧测试基础设施问题：波形测试仍断言旧 attack/decay 参数且未固定软件场景图；10K 导入测试进程包含多组用例和异步清理，CTest 看门狗与全套命令统一为 180 秒，但单个 10K 完成信号与模型插入性能断言仍保持 30 秒。业务断言未放宽。
- 本轮复核还确认 `separation_process_client_test` 的超大尾部用例把 Worker 握手压到 150ms，完整套件负载下会先误触握手超时。测试现先等待真实进度消息，再要求 500ms 内拒绝超大尾部；生产协议时限未改。该测试连续 10 次和最终全套均通过。

### 2026-08-31 同步多轨试听最终收口

- 新增轻量原生 `StemPreviewMixer`，只在结果试听时创建一个输出设备；各结果轨继续复用现有 `AudioEngine` 解码，不引入第三方依赖，也不进入播放器核心状态机。
- 底部绿色播放键同步播放全部可用结果轨。MDX 使用人声与伴奏；完整 Demucs 使用人声、鼓组、贝斯和其他并排除派生伴奏，避免重复叠加；组件不完整时安全回退到人声与派生伴奏。
- 点击任意结果波形会切换为该轨独奏，并从点击位置开始；再次使用底部播放键会在保留位置的前提下回到多轨混音。上方橙色输入试听保持独立位置和播放状态。
- 每轨增益实时进入混音；底部混音始终按全部可用结果轨构建，与导出勾选状态独立，不支持或缺失的轨道不会被伪造播放。混音器在渲染前拒绝非 44.1 kHz 立体声源，并覆盖短尾块、自然 EOF、Unicode 路径及非双声道错误路径。
- 本次新增 `stem_preview_mixer_test` 后 Release 测试清单为 89 项；上述 89/89 是本节修改后的新鲜全量结果，不把未安装模型时快速跳过的 `separation_real_model_test` 冒充本轮真实模型推理。

### 2026-09-01 自定义模型目录与视频输入收口

- 自定义模型卡新增并列的“打开模型目录 / 更改目录”。用户选定的绝对路径以 `QSaveFile` 原子保存到分离数据目录，重启后恢复；切换目录不复制或迁移大型模型文件。
- 模型目录使用 `QFileSystemWatcher` 事件驱动、700ms 防抖扫描，不增加轮询或常驻 Worker。支持官方 catalog 文件直接放在所选根目录，也兼容原有的模型 ID 子目录。
- 自动识别仍受安全边界约束：先按 catalog 文件名和字节数筛选，再异步核对 SHA-256；未知 ONNX、文件不完整或哈希不符时不会标记安装、更不会执行。手工放在根目录的模型只允许用户在文件管理器中删除，避免程序误删同目录其他文件。
- 文件选择器与拖放提示补充 MP4、MKV、MOV、WebM、AVI、M4V、MPEG/MPG、TS。底层不增加解码依赖，继续由现有 FFmpeg `av_find_best_stream(AVMEDIA_TYPE_AUDIO)` 从容器选择音频流。
- 备用模型弹窗已写入用户指定的百度网盘说明、链接和提取码，并保留文本选择复制能力。
- 1672×941 模型卡宽度按主工作区自适应，默认即可完整看到两个目录按钮；额外保留横向滚动余量，较窄窗口及未来 catalog 扩展仍可使用滚轮或滚动条。
- 实机证据位于 `build/qa/custom-model-video/`：`中文视频输入.mp4` 是 4 秒 AAC 音轨视频夹具，`1672x941-video-input-final.png` 显示生产页面真实读出文件名、大小、时长与统一波形；`runtime-final.log` 无 QML 运行时警告。
- 聚焦回归覆盖自定义目录持久化、Unicode 路径、受信文件自动识别、未知 ONNX 忽略、活动任务期间拒绝切换目录、视频格式过滤、备用链接文本及主尺寸按钮可见性。
- VS2022 x64 Release 全量构建退出 0；新鲜完整 CTest 为 89/89 通过、0 失败，总耗时 112.31 秒。

## 未关闭风险

1. 必须用已安装模型和真实完成结果重拍与参考图相同状态，再做同输入视觉判定；当前不能写“最终视觉通过”。
2. 真实声卡/扬声器试听切换、人工点击导出/加入播放列表/打开目录仍需设备与人工验收；自动测试只证明控制器和文件路径行为。
3. 未在真实磁盘耗尽环境破坏性填盘；磁盘不足使用安全注入和事务测试覆盖。
4. 未验收 macOS ARM64/CoreML，不对外宣称支持。
5. TRvlvr 模型权重缺少单独明确的商业再分发许可；当前只按需下载不随包分发，正式商用仍需权利人/法务确认。
6. 正式安装包未生成，符合发布门禁。
