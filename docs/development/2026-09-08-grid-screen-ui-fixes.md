# 2026-09-08 人工测试反馈修复

基于 `main@86a8458` 完成本轮修复。后续用户授权集成打包，纳入沉浸视觉已提交成果 `8912f13`；历史工作树的未提交对照文件不纳入。

## 本轮范围

- 文件名处理：占位文字改为“输入要添加的前缀/后缀”，保留原处理规则。
- 歌曲列表：当前播放的三柱指示使用公共主题强调色。
- 专业模式：完整波形接入周期相位估计，在原 BPM 附近拟合小数 BPM，不改音频文件或播放速度。人工校准优先；低置信度仍是估算。
- 专业模式载入曲目后，首次可靠分析或已有人工首拍可自动定位到播放头，保持播放状态。手动播放、暂停、跳转、CUE、擦盘及退出专业模式取消本次自动定位；后续再次载入仍有资格。
- 切主题保留当前屏幕/锚点和各主题用户尺寸；跨 DPI 使用逻辑尺寸，避免列表行数变化。超出工作区时仍允许合理约束。
- 共享运行环境：显式主题色进度条；单任务隐藏滚动条，溢出时使用细条并为按钮留出空间。
- 公共确认弹窗：标准按钮跟随中英文语言，缩小短确认窗口，修复标题颜色。
- 打包前追加：添加标签、重命名标签、删除标签和列表右键自定义标签统一 320px；复用 ThemedTextField，删除统一“确定/取消”并保留不删除音乐文件的说明。四入口真实 QML 回归及中文截图通过：`20260908-tags-readable.txt`、`20260908-tags-readable.png-*.png`。
- 集成审查修正沉浸频谱静音门限与上游 0.002F 发布门限不一致的问题，增加淡出残值数据驱动回归；防止静音后继续触发正强度节拍。

## 验证证据

- Release 应用及相关测试目标构建通过：`build/qa/20260908-acceptance-build.txt`。
- 播放控制全套（含新增自动定位场景）通过：`build/qa/20260908-acceptance-regression.txt`。
- 主窗、专业模式 QML 回归通过：`build/qa/20260908-firstbeat-green-regression.txt`。
- 网格数学、窗口控制 offscreen、翻译和文件名处理通过：`build/qa/20260908-final-regression.txt`。该轮并非全部通过，后续修正了测试夹具问题。
- 副屏主题切换、最小尺寸保存顺序、跨 DPI 列表贴合三项原生测试全部通过：`build/qa/20260908-window-native-final.txt`。
- 已目视复核前后缀布局、深浅色下载进度和中文确认弹窗截图（`build/qa/20260908-*`）。
- 伴奏分离与文件名页面最终回归 2/2 通过：`build/qa/20260908-runtime-final-regression.txt`。测试夹具现持久保留注入的卡片快照，避免后台硬件发现覆盖 UI-only 数据；未改变下载业务逻辑。

## 边界

## 集成候选验证

- 合入沉浸视觉 `8912f13` 后全部 Release 目标构建成功：`20260908-latest-final-build.txt`。
- 全量 CTest 首轮 184/186 通过（909.17秒），保留两项失败记录：`20260908-latest-full-release.txt`。
- 两项为旧测试合同：QML冲击测试只提供BPM却断言有声冲击，现补实时频谱并新增静音强度归零断言；琥珀电影卡片仍要求旧高度56，更新为用户截图52，保留点击与键盘断言。没有更改生产代码迎合测试。
- 修正后四项相关测试全部通过（13.68秒）：`20260908-latest-recheck.txt`；不是重新完整运行186项的结果。
- 三项副屏/跨DPI原生回归通过，5通过0跳过（含初始化/清理）：`20260908-latest-native-windows.txt`。
- 独立集成审查发现的静音门控问题已修复，音频视觉控制器和GPU测试均在上述全量运行中通过。最终Diff检查无空白错误。
- 安装包保持1.0.0候选版，不远程推送或上架；旧桌面安装包保留。大型真实模型、整曲分离和最终听音仍需人工验证。

自动结果是拍点相位估计，不是所有风格的小节首拍识别。分析使用前 120 秒；反拍较强、变速及复杂音乐仍可能需要人工校准。尚未完成真实曲目逐首听音验收，不承诺每首歌完全自动对准。原生窗口全套旧测试中另有客户区/外框坐标差异，本轮仅对相关原生路径作通过声明。
