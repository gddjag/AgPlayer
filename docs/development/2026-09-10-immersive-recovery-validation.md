# 沉浸视觉恢复与验收记录

## 工作区与边界

- 基线：`92959bb0a13bfec8685752bf007a6222014a4032`。
- 主线清理保留的修复工作树：`C:/Users/Administrator/.codex/worktrees/aeb5/AgPlayer`。
- 独立 Release 构建：`D:/ai/agplayer-immersive-recovered-build`，不向主线添加构建产物。
- 原版对照固定为 Sonic Topography `ec8ecbaec0c9c5094b6b1480df0d6b2d32d6349b`。
- 保持原生 QRhi、共享音频链和播放器布局。不打包、不发布。

## 根因与修复

1. 最大地形允许 224×224，但实例容量按 192×192 分配；截断后地形计数未同步，额外实例绘制数可能无符号下溢。扩容至覆盖既定上限，并防御性约束绘制计数。
2. GUI 请求帧之后渲染器再次跳帧，新目标可能没有有效画面。统一 GUI 定时调度，已请求的 RHI 帧完整绘制。
3. 密度连续输入合并至最多每 100ms 重建一次，等待时绘制旧完整布局；新资源强制上传，布局未更新不发布稳定帧标记。
4. 沉浸宿主脱离被隐藏主窗口的 transient ownership；全屏保存/恢复窗口几何，保留独立任务栏恢复入口。
5. 沉浸进入状态不再持久化，读取时移除旧 mode；其余用户参数仍保留。
6. canonical 主题的原版材质和几何此前仅用于测试标志，实际运行走 native 艺术材质。统一 canonical 运行与原版采样路径，保持用户要求的静音地面稳定。
7. 三主题共享静态眼睛图标，画质标签标注帧率而非与密度无关的网格数。
8. 预设标签复用 Theme 的对比色函数，修复第 5 个浅色按钮白字不可读。
9. 退出卸载 Terrain Loader、停止三个界面定时器，专用沉浸窗口不持久缓存隐藏场景；普通播放器窗口策略不变。最小化保留 UI 对象，重新显示时正常恢复。
10. 首次真实音乐长测发现每隔 1–8 秒音频重置。原 PCM tap 只保留最新 1024 采样，稍迟的 GUI 轮询便丢失连续采样。改为现有环缓冲 FIFO 消费和最多 16 次有界读取，未增加音频核心、缓冲容量或 ABI 字段；真实 seek/disable/overflow 仍保留代际重置。
11. 沉浸底部波形显示强度由 0.58 提到 0.74，增大合成后的进度明暗差；保留频段颜色、共享数据和用户设置的未播放区亮度，不新增波形图层或后台任务。

## 当前验证（不是发布结论）

- Release 应用和相关测试目标构建通过。
- CTest：预设目录、参数保存、地形状态、音频接入 4/4 通过。
- QML 沉浸集成 44 通过；原生 Windows 全屏/Esc/最小化恢复专项 3 通过。
- GPU 首轮：33 通过、1 失败、1 可选性能探针跳过；高密度/画质切换通过。
- 首轮剩余失败为旧 native 亮区覆盖契约。保留旧断言，将该测试明确限定为 manual/native；canonical 以独立原版高度、顶面基色和边缘样本校验，不把两种材质的艺术标准混用。
- 完整材质采样：177 通过、0 失败、4 可选项跳过，262.9 秒。
- 旧 native 曝光用例明确固定历史默认振幅 34、柱宽 95 后 3/3 通过；未调整亮度阈值，不把它当作 canonical 整场景一致证据。
- 退出生命周期：QML 集成 44 通过；加强后的真实 D3D11 三轮退出测试先 RED（没有场景失效），修复后 3/3 通过，三轮均在退出时记录 renderer 释放，播放状态/曲目不变。
- PCM FIFO 延迟采样与真实播放延迟测试先 RED，修复后 tap、音频特征控制器、Terrain item 3/3 通过；并发 payload/epoch 用例通过。
- 已生成 13 个 1920×1080 实际预设截图；第 5 个标签改色后重新截图确认可读。截图为合成输入功能核验，不是与原版相同真实 PCM 的动态逐帧验收。
- 初次长测因发现真实音乐重置主动中止，记录保留。修复后真实音乐运行满 1799.995 秒，正常退出；112,080 个原始帧间隔中位 16.079ms、P95 19.057ms、最大 426.885ms。运行期间发生歌曲、密度变化及一次宿主恢复，按交互长测记录，不冒充固定输入基准；长帧不剔除。
- 30 分钟中专用资源 generation 始终为 1、运行 item 始终为 1；GUI 每秒探针最大延迟 2ms（不是按钮端到端延迟测量）。进程内存随曲目/场景有波动，最后约 3 分钟私有内存保持约 401MiB；不能凭本轮宣称全面无泄漏。
- 最大密度短测补齐：默认 24,025 柱、最大 50,176 柱，1920×1080 及原生 3440×1440 都持续出帧；最大密度各 120 帧块 P95 22.17–22.35ms，3 秒观察段均有 184–186 帧。5/5 通过，合成输入短测，不替代真实音乐长测或 GPU 时间戳。
- 最终 QML 回归 44 通过、0 失败、1 GPU 专项跳过；D3D11 专项另行 3/3 通过。后者使用 30 秒真实 PCM 静音文件与空输出设备，三轮退出后均验证 Playing、同一曲目、进度继续变化，并有 renderer 释放日志。原 1 秒测试文件在检查结束前发生曲终切换，已隔离这一无关队列边界，未修改生产播放逻辑。
- 最终 Release 构建与 Diff Review 通过；本次未增加运行时依赖、播放核心或常驻任务。
- 最终深/浅沉浸截图确认波形可见度与预设文字；classic、integrated、rolling 三种播放器实际截图均确认共享眼睛入口。证据在 `presets-final/`，不是合成的界面效果图。

## 证据与限制

本轮日志保存在独立构建目录。历史短测不能替代本轮 30 分钟验收。
本机实际显卡此前识别为 RTX 4070 Ti SUPER，不将其称为普通 RTX 4070 实测。
GPU 时间戳不可用时记录 -1；CPU 命令录制耗时不代表 GPU 耗时。
单柱数值对照不证明完整场景像素、动态效果或跨平台完全等效。
完整 13 预设相同真实 PCM 动态对照、普通 RTX 4070（本机为 Ti SUPER）、严格固定场景 30 分钟基准以及偶发长帧归因仍未验收。实际黑闪的逐帧视觉监测未做，不能仅凭帧计数宣布完全没有黑闪。

## 后续动态对齐：范围与已确认差异

- 固定提交的 `MapScene.tsx` 流星落地使用白色冲击波类型；原生生产事件此前始终正强度，无法进入着色器已有的负强度白波分支。修复限定 canonical 落地事件，普通波、手动材质和既定事件间隔保持不变。
- FIFO 补读此前每块都推进 FFT 平滑、描述符与鼓点历史，一次轮询可能推进多次。接收连续 PCM 与分析分离，轮询结束只分析最新完整窗口一次；鼓点使用分析间的单调时间，首次 1/60 秒、间隔上限 0.25 秒。未增加线程、播放核心或采样缓冲。
- 这只是消除音频分块倍增，不是原版 RAF 完全等效：目前仍为约 17ms GUI 分析时钟，而非实际渲染帧时钟。不同采样率的分析频段与完整逐帧响应仍需单独对照。
- 预设切换的八个颜色角色及发光统一使用原版 `clamp(3*delta)` 逐帧渐变，在现有 sRGB 输入与着色器线性化之间正确转换；标志位不参与渐变，首帧直接初始化为当前预设。manual 保持原有渐变逻辑，静态终态配色不变。
- 共享眼睛入口改用用户提供的 SVG 原始路径，继续使用主题颜色。此前三主题截图统一强制 1280×900，经典主题截图高度不代表生产默认；代码默认仍为 863×266。

本次针对性证据：`meteor-red.txt` 落地白波用例失败，修复后 `meteor-green.txt` 连同普通鼓点亮起/回落回归共 4 通过；`pcm-once-red.txt` 检出单次轮询多次分析，`pcm-once-green.txt` 控制器 30 通过；`palette-red.txt` 检出颜色角色/空间差异，状态测试修复后 62 通过。以上仍不是 13 预设完整动态逐帧验收；此前 30 分钟长测也不自动覆盖此次新修改。

最终 Release 构建 `dynamic-align-final-build.log` 通过；加强为三频+包络 PCM 的分块一致性测试后，控制器、状态、Terrain 接入三个 CTest 目标再次全部通过。`palette-gpu-green.txt` 的 D3D11 普通鼓点、流星落地、场景启动及材质参数回归 15 通过。已检查最终 Diff；未打包、未合并主线。颜色过渡目前有数值测试和渲染接线审查，没有原版/原生同一切换时间表的 GPU 逐帧对照片段，仍需后续视觉验收。

## 实际渲染帧时钟接入

后续已替换上述沉浸路径的 GUI 分析时钟：GUI 只收集连续 PCM，`synchronize()` 在 GUI 阻塞期间复制纯值窗口、采样率和本地 epoch，renderer 独占无分配的 `VisualAudioFrameAnalyzer`，每实际绘制帧执行一次 FFT、描述符和鼓点分析，再推进地形。同一窗口没有新 PCM 时仍按帧推进平滑；无效窗口不会生成虚假鼓点。没有新增线程、播放核心、QML 公开遥测属性或二次跳帧。

- GUI 接入采用引用计数抑制重复分析，源替换/析构配对释放；输出仅排队回报 GUI，含活动、重置和模式过期检查。
- 恢复/renderer 重建保留已消费事件序号的下界，防止旧 GUI 回报被丢弃后新鼓点被吞。两项竞态先 RED（`terrain-cadence-race-red.txt`），修复后 item 22/22 通过。
- 控制器/纯分析器 32/32 通过；实际 D3D11、48kHz 测试 PCM 在 Eco/High 下验证绘制帧与分析回报对应，隐藏停算、暂停清零测试通过。测试 PCM 为 80Hz WAV，不冒充真实音乐长测。
- `frame-clock-final-build.log` Release 构建通过，控制器、状态、item 三个 CTest 目标再次通过。尚需本次版本的真实音乐长测。
- 独立固定原版与导出器恢复到 `D:/ai/agplayer-reference-ec8-20260910/`。13 预设各 1/2 秒真实 demo PCM 导出和 native uniform 重放共 26 对；这是材质/几何的同输入重放，不证明生产 PCM 时刻和完整事件长片已等效。原版 PNG 为 straight alpha，原生诊断读回涉及 premultiplied alpha，比较必须先统一解释。

### 开发侧原版真实 PCM 导出工具

`scripts/qa/sonic-reference-export.mjs` 与同名 `.tsx` 仅作为开发侧工具，必须提供独立可写的原版 study（固定提交 `ec8ecbaec0c9c5094b6b1480df0d6b2d32d6349b`，已安装其锁定依赖），不向播放器复制原版代码、音频、浏览器或依赖。先在 study 运行 `npm run dev -- --port 3017`，再执行：

```powershell
node scripts/qa/sonic-reference-export.mjs D:/ai/agplayer-reference-ec8-20260910/study D:/ai/agplayer-reference-ec8-20260910/traced-snapshots C:/Users/Administrator/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright 3017
```

工具使用已安装 Edge，调用原版 AudioEngine / MapScene；默认仅导出 nocturnal，可在端口参数后指定 `all` 导出13预设，每次仅写一份共享音频 trace。校验固定参考的5项SHA256，拒绝覆盖已有输出或不同内容的harness。48kHz OfflineAudioContext 每768样本推进，另外插入48000检查点（47616→48000→48384）；96000为第125个常规步。输出时刻取实际 `ctx.currentTime`，并非名义标记；实测1s/2s累计63/126次分析。每次 advance 前清除 AudioEngine 帧缓存；生产解码器链的窗口时刻需另行对照，不能从同uniform画面直接推定通过。

本次26对旧导出按正确预乘解释，前景交集RGB MAE范围0.103–0.626/255，mask IoU为0.997575–0.998243；不是设定合格阈值。全部 native 重复帧严格断言仍失败（43–162像素，原始RGBA最大差1–31），不将平均误差代替该失败。原版静态地面例外、浮块关闭、随机事件未固定种子及仅两个时间点均限制验收范围。新版诊断 PNG 已修复alpha标记时，不再二次解预乘。

### 本次真实音频与长测结果

- `real-audio-30m-render-clock` 实测约1800秒正常退出，记录112320帧间隔，中位16.033ms、P95 19.095ms；资源代数始终1。用户确认期间手动调整密度，故归为含交互长测，不作为固定默认密度基准。
- 最大310.464ms长帧位于北京时间约09:57:26.899，附近仍24025柱；首次变为50176柱在10:10:00.688–10:10:02.613，不能将该长帧归因用户调整密度。全程7个不低于50ms的间隔，其中5个不低于100ms，原因仍待核查。帧统计不能替代黑闪逐帧监测或退出后GPU释放验收。
- 新原版导出 `trace-after-soak/audio-trace.json` 包含48kHz真实demo的126帧、126次频谱读取。原生CPU逐帧对照全部512频谱字节精确相等，21项descriptor/kick在1e-6内通过。直接通过，未修改算法或放宽断言；这证明相同PCM窗口和时间输入下的分析等效，不证明生产播放时两端取得PCM窗口的时刻已经等效。
- 完整控制器与真实trace回归34/34通过、无跳过，日志`pcm-real-trace-green.txt`；共64512频谱字节零差异、2646项特征/鼓点数值比较通过。
- `repeatability-moss-hash-1x/4x` 用同一full-varying hash着色器隔离材质：1x四帧严格通过，4x仍有4–12个非不透明像素变化，不透明像素变化为0。证明存在独立于材质计算的MSAA边界残差，不能据此解释旧材质31 LSB异常，也没有放宽严格断言或修改生产材质。
- 后续hash专用`AGPLAYER_PARITY_HASH_BLEND_OFF=1`单变量实验仍在4x原始字节重复断言失败；manifest确认关闭混合、四帧uniform hash相同、读回完成。混合并非该残差的必要条件，覆盖/resolve仍待隔离。开关仅诊断有效，生产及默认测试行为不变；blend-off输出PNG仍按预乘导出，不用它比较颜色，只采用raw readback结果。
- `AGPLAYER_PARITY_HASH_RETAIN_RESOLVE=1`仅诊断保留首帧resolve纹理，随后三帧重新读取：4x/blend-off原始字节严格比较3/3通过。结合重新绘制失败基线，将问题收窄到重新绘制/resolve阶段，不能据此区分覆盖与resolve，也不证明用户画面影响。
- 原版`context-samples/nocturnal-48000.json`实际查询默认framebuffer为4 samples、1 sample buffer、1920×1080/DPR1；alpha/antialias/premultipliedAlpha均true，Three色彩空间srgb、toneMapping枚举4、exposure1。这是实际上下文记录，不以请求antialias推测采样数；也不据上下文色调映射设置推断自定义shader必然执行相同转换。
- `repeatability-moss-geometry-baseline-4x`逐帧记录确认四帧target/resolve、generation=1、尺寸/采样数及顶点/索引/实例CPU源hash固定，静态几何仅首帧上传；正常材质严格重复仍失败。因此此运行排除了目标重建、resize或CPU上传源变化，尚未验证GPU buffer内容，不把它归结为已确认驱动缺陷。
- `terrain-response-trace/audio-trace.json`新增原版实际MapScene回调delta及每帧15项uniform输出；126帧1890项原生地形响应CPU比较在1e-6内通过。初帧静音不能检出生产接线复用Kick首帧1/60给地形的问题，该边界单独修复/验证。
- 首帧接线现已修复：真实renderer调用的私有分析/响应路径将实际wall delta交给地形，Kick的首帧1/60及后续限幅仍由音频分析器独立处理。非零首帧用例先RED（0.0868498666061 vs 0.084171108108），修复后首帧/次帧/长帧/重置回归及item全目标23/23通过（`terrain-first-dt-green-software.txt`，按该目标规定的软件后端）。误用D3D11运行软件专用目标产生的2项后端预期失败另留日志，不冒充GPU验收。`terrain-clock-final-app-build.log`主程序Release构建通过；本次时序小修复尚无更新后的30分钟长测。

### 退出后共享分析需求交接

复审发现既有两个writer需求不一致：Surface析构强制关闭分析会覆盖rolling需求，而Main使用面板显示偏好推断渲染会在关闭后误启分析。Main改为实际`terrainItem.renderingRequested`，独立Surface启停/析构保留rolling需求；未新增线程或播放接口。

真实D3D11先复现退出rolling后分析关闭（`rolling-analysis-red.txt`），修复后三模式需求及PCM继续处理5/5通过（`rolling-analysis-green.txt`），原有三轮最小化/恢复/退出播放继续专项3/3通过（`rolling-analysis-minimize-green.txt`）。测试harness存在原有未注入模型警告，不代表全部主题UI验收。`lifecycle-final-app-build.log`主程序Release重链通过，更新QML已进入构建产物。30分钟长测早于此QML交接修复，不宣称覆盖修改后的长期运行。

### 完整场景审查确认的剩余差异

- 原版鼓点波、军鼓白波与流星独立；原生曾以`!meteorsEnabled`禁止普通波。本次已修复普通鼓点波互斥，保留共享事件间隔及流星占用门限/落地白波。GPU先RED（changedPixels=0），修复后1300像素变化，连同落地白波和鼓点亮暗专项5/5通过（`beat-meteor-green.txt`）；`beat-wave-final-app-build.log`主程序Release构建通过。
- 独立Snare检测与白波路由已接通：复用同帧512bins，原版完整AudioEngine生成的160帧边界与126帧真实音乐事件/强度在1e-12内匹配；真实事件在0-based帧12/123。生产helper读取同窗口、无效/重置清零，白波半径10–45、强度min(raw*3,3)，不占用impact/8拍计数。检测保留原版30分析帧hold，展示按用户3–6秒共享门限，落地白波优先并重锚门限。controller37/item25/state62及D3D11专项5项通过（`snare-*-green.txt`）。GPU专项证明共享白波通路及实际PCM帧分析，不冒充Snare专属实景录像；主程序需在本批集成后统一重编。
- 流星核心运动现已改为原版公式：半径0–25、高度30–40、速度`1+u*0.5+strength*1.5`，CPU落地与GPU位置使用同一轨迹，落地白波同xy且强度至少1.2。16字节内部UBO追加在offset752（总768），不改变旧字段偏移。最终状态62/62、GPU4/4通过（`meteor-motion-final-*`）。尾迹/碰撞仍为现有简化粒子，不宣称完整模拟等效。
- 浮块的独立大小/速度/强度设置与包络运动在下述批次补齐；完整13主题浮块逐帧像素仍未验收。固定地面与稳定运镜是用户明确例外，继续保留。
- 共享PCM tap在设备callback内、EQ/过渡后、音量/静音前，代表待输出数据；其sample index仅连续性序号，不是曲目时间或硬件已播放位置。现有同窗口数值对照不证明真实设备时刻完全对齐，不能将该索引除采样率冒充播放时间。
- 固定原版AudioEngine的分析链为source→fade→analyser，音量在另一输出分支；native音量独立性一致。跨实现真实PCM对照需关闭播放器音频EQ、速度/音高中性并避开过渡，视觉EQ仍使用参考参数。离线导出绕过fade，尚未覆盖原版起停淡化，不据此改写共享播放核心。
- 4x保留同MSAA附件重复resolve严格通过，重新绘制仍失败；hash诊断也出现不透明像素差（最大12/32码值），因此不仅是低位舍入，材质计算及透明混合不是必要条件。继续定位光栅/深度可见性，不据此直接认定驱动问题。
- 原版深度比较LEQUAL、原生默认Less确有差异；仅测试改LEQUAL仍严格失败（83–128像素、最大31码值），未解决重复性问题。实验已撤回，不让测试默认与生产不一致；该状态差异是否需生产修正仍需独立视觉证据。
- 诊断工具已修正blend-off PNG的关联色解释，明确非外观证据，并拒绝同时开启两种resolve隔离开关。测试程序已重建恢复默认Less；默认alpha导出3/3通过，冲突开关在创建窗口前按预期拒绝。此前blend-off图不可用于颜色判断，raw重复性结论不受影响。
- 实例Immutable一次上传的4x控制仍严格失败，不能归因实例Dynamic重命名。NVAPI只读查询全局/基础MFAA均关闭，测试与主程序未发现独立profile；没有修改驱动设置，当前证据不支持MFAA开启假设。

### 浮块设置与实际界面接入

新增并持久化原版四项0–100设置：最小尺寸9、最大尺寸26、跟随速度77、响应强度55。复用动态页、Theme和现有滑条；CPU包络采用实际帧delta，GPU类型1使用原版布局、XYZ旋转与独立材质。UBO在768偏移追加16字节浮块参数，总784，旧字段偏移不变。没有新增依赖、线程、播放接口或额外FFT。

`floating-integrated-app-build.log`记录Qt6.7主程序及QML测试目标重编通过。真实demo通过主程序共享播放链生成`floating-live.png`，1920×1080、D3D11、截图时236帧/resources1；只证明此时画面及运行，不代表长期无黑闪。主程序与原版完整时间轴对照尚未完成。

新增真实QML控件输入回归先遇到窗口展开尚未完成的测试前提失败，等待布局就绪后，不直接设置tab/强制focus，实际点击、键盘、拖动及后续绑定同步3/3通过。Windows+software截图`floating-controls-panel-windows.png`已查看，四项中文标签及数值清晰；offscreen截图字体为方框，仅保留功能结果，不用于视觉验收。

首轮`floating-*-green.txt`实际加载Qt6.8.3 DLL，不能冒充主程序Qt6.7验证。随后从主构建重编三个目标，显式使用Qt6.7 bin/plugins/QML复验：`floating-state-qt670.txt`62/62、`floating-params-qt670.txt`3/3、`floating-gpu-qt670.txt`8/8通过。四参数实际改变像素分别2056/35707/24589/26295；只用来证明参数可见，不是原版像素相等判据。旧日志和版本差异保留。

浮块集成后Qt6.7的`qml_immersive_integration_test`再次通过：44通过、0失败、1平台专项跳过（`tests/qml-immersive-integration.xml`）。该软件后端回归不替代D3D11生命周期或完整动态验收。

### 流星尾迹与落地粒子

原有固定尾线、环段及径向爆散(kind4/5/6)已移除，改用renderer自有200槽环形池。每实际帧飞行尾迹概率0.7、落地10粒；位置随机偏移、速度、0.5–1秒寿命、线性缩小及出生当帧推进按固定原版公式实现。复用实例缓冲尾段，不因粒子运动重传地形；关闭、恢复、epoch重置处理仍在同一renderer生命周期内。单流星和3–6秒间隔继续作为用户要求的例外。

Qt6.7数值测试先RED（`meteor-pool-red.txt`两项失败），随后`meteor-pool-state-final.txt`64通过；`meteor-pool-item-final.txt`24通过、1未提供可选PCM文件的跳过；`meteor-pool-gpu-final.txt`5通过。GPU证明池实际上传、200槽、关闭清空和资源代数稳定，并回归地形白波/普通波。测试已重命名为`meteorParticlePoolUploadsAndClearsWithoutResourceRebuild`，避免名称暗示已证明粒子像素运动。原无oracle支持的亮度阈值失败和像素0诊断保留，不降阈值冒充视觉等效。

kind2/7使用unlit、粒子alpha0.6、颜色编码后雾处理。尚未验证原版初始白色材质向目标色过渡的逐帧差异，也未做粒子同随机种子像素oracle；生命周期通过不替代这两项。独立只读审查无阻断；本批最终主程序重编与最新长测尚待后续集成。

### 高振幅响应与频段开关

源码审计确认原版幅度在50以上按`1 + ((a - 50) / 50)^2 * 14`放大，而native canonical此前始终为线性`a/100`后在shader乘2，造成75仅1.5倍（原版4.5）、100仅2倍（原版15）。修复放在canonical主题适配边界：UI/持久值保持0–100，snapshot在0/50/75/100分别为0/.5/2.25/7.5；manual继续旧线性语义，导入oracle半幅值不变。Qt6.7测试先RED（75实际.75、期望2.25），修复后item 25通过、1项可选PCM跳过，state 64通过。GPU及主程序重编尚待本批最终集成，不据CPU测试宣称画面已通过。

原版8频段各有独立enabled状态，可保留增益值后临时关闭。此链现已接通：默认八项全开，单独持久化`visualEqEnabled`，关闭不改`visualEqGains`；Windows INI把布尔列表重载为字符串的问题通过仅接受精确`true`/`false`兼容，其他类型安全回退true。QML每行复用紧凑主题复选框，objectName为`visualEqEnabled_0..7`。控制器专项3/3、item 26通过/1可选PCM跳过；Windows software真实点击首尾频段、增益保持及外部更新后绑定3/3通过（`eq-enabled-qml-green.txt`）。独立审查未发现预设切换覆写开关或数据链断裂。

集成后主程序、QML测试和相关Qt目标统一重编通过（`eq-enabled-integrated-build.log`）。Qt6.7 D3D11动态专项19/19通过，涵盖200槽粒子生命周期、落地白波、普通波、四项浮块参数和材质控制（`dynamic-align-gpu-final.txt`）；其中粒子上半区亮像素诊断仍为0，不是原版视觉oracle，也未被当作通过条件。完整QML回归首次因歌单抽屉在软件整套负载下用固定170ms采样状态失败；单测与相邻用例隔离通过。保留140ms产品配置断言，改为最多1000ms等待实际状态后，完整回归通过；未修改生产抽屉行为。

最新Qt6.7主程序使用真实demo、D3D11、1920×1080在截图时正常出帧并保存`latest-dynamic.png`；动态页截图`dynamic-panel-latest.png`确认浮块四项和频段开关布局可读。静态截图不证明节奏、粒子生命周期或长期稳定性。

### 预设自动轮换

固定原版提交的轮换功能是可开关的3–120秒定时轮换，默认关闭、默认间隔10秒，并非按换歌触发。按产品要求，原生界面现同时提供“定时”和“换歌”两个独立开关：新版本迁移后默认仅开启10秒定时轮换；换歌轮换默认关闭。两者只在沉浸模式开启时运行，顺序遍历13个内置预设；换歌路径只接受不同的非空曲目ID，暂停、进度变化、同曲重复及沉浸外切歌不会误触发。轮换由QML展示层调度，旧shader内部连续色相循环被明确关闭，避免一项开关产生两种动画。

Qt6.7控制器与完整沉浸QML回归通过：控制器验证默认值、3/120秒边界及持久化；QML验证开关和间隔控件存在、关闭时不切、不同曲目只切一次、离开沉浸不切，以及3秒定时实际从`ink-wash`切至`nocturnal`。Release主程序重编通过（`theme-cycle-final-build.log`）。Windows+D3D11截图`theme-rotation-panel.png`为1440×900，预设页两个开关、10秒间隔和13张卡片均可见；截图时renderer为Ready、resources=1。该短时截图不替代长时间轮换稳定性验收。

### 静音时视觉连续性

审计确认视觉PCM位于播放器音量/静音处理之前，但旧`set_muted()`复用了同时使visual tap换代的`invalidate_output_meter()`，导致静音和取消静音时FFT、鼓点包络、地形响应及粒子状态被错误重置。现将输出电平计量失效拆成独立路径：静音仍立即清除扬声器电平读数，但visual PCM generation与样本序号保持连续。暂停、seek、换曲、设备丢失和设备切换仍走完整失效路径，未放松真实音频不连续边界。

`audio_engine_test`先以静音前后generation/样本连续断言复现失败，再修复通过；`output_level_c_api_test`继续证明静音输出电平为零且静音期间仍有视觉PCM。独立调用链复核未发现旁路静音入口或误删真实中断清理。本批主程序重链及修改后的长期真实播放尚需在后续统一执行。

补充C API边界断言后，静音和取消静音均再次核对visual PCM generation不变；`audio_engine_test`与`output_level_c_api_test`共2项通过（1.59秒）。Qt6.7 Release主程序已重链，不能将此短测写成更新后的30分钟验收。

### 清晰度与浅色预设曝光

Qt6.7/D3D11固定静态冰川白昼用例证明旧清晰度1.40把完整材质直接乘亮，取样区域平均亮度相对中性1.14上升20.35%，属于曝光变化而非柱体清晰度。修复后1.14仍走零差值的原版材质路径；提高到1.40只增强顶面边界与侧面肩部的局部对比，平均亮度增幅降为3.64%，同时19452个取样像素发生可见变化。夜景`column-clarity`专项653像素超过可见阈值，避免以“防洗白”为由使滑条失效。新增浅色预设防曝光回归与既有材质参数专项均通过，完整GPU测试110最终106.26秒通过。

### 4x MSAA深度诊断

保持4x、深度测试/写入、blend-off hash材质时，单中心柱与相隔约8单位的九柱连续四帧raw RGBA均严格一致；完整密集fixture仍存在极少数像素变化。此前关闭深度测试或仅关闭深度写入也会严格一致，结合fixture不含独立地面/浮块/特效，将范围收窄到密集柱之间的遮挡/深度可见性竞争，不是材质、实例上传、资源重建或独立地面。不能为消除诊断像素而关闭生产深度，否则会破坏正确遮挡；当前保留生产行为并继续把该项列为未完成的严格逐帧对齐。

### 最新短时真实播放稳定性

修改后的Qt6.7 Release主程序以D3D11、1920×1080和真实`demo.mp3`分别完成冰川白昼75秒及夜景180秒运行。夜景记录11027帧，四次renderer采样均present/active/exposed/itemLive/items=1且resources=1，正常退出；61秒与122秒的私有内存为370778112/370769920字节、句柄906/905，说明首分钟约49MB到371MB是一次性预热平台，后续两分钟未继续增长。该结果覆盖最新静音连续性与清晰度改动，但仍不是计划要求的RTX 4070固定默认密度30分钟验收，也未单独量取进程退出后的GPU资源释放。

同一集成产物的`terrain_reactor_gpu_smoke_test`完整目标106.26秒通过，`terrain_column_material_test`完整目标317.29秒通过；后者包含材质、顶面、边缘和原版重放基线。严格4x密集场景重复性仍按上一节保留为已知未对齐项。

### 暂停释放、恢复连续性与95%收口

暂停不再把共享视觉PCM、频谱历史、鼓点包络和地形状态当作曲目边界清空。渲染器保留最后一个有效窗口，按原版约1.6秒视觉释放衰减；恢复播放沿用连续sample index和generation，不再先黑场再从零顶起。seek、换曲、设备切换和设备丢失仍执行完整失效，避免把真正的不连续音频误接成同一时间轴。静音继续只清扬声器电平表，不打断视觉。

最终针对性回归在Qt6.7 Release通过：核心音频、C API、电平/PCM控制器、地形状态与item共5/5；D3D11专项覆盖真实PCM实际渲染帧、暂停释放/恢复、隐藏停算、密度与画质切换、顶部高频亮面、中心内光及普通鼓点波纹。共享材质专项覆盖主题线性色域、参考uniform映射、柱体内光不洗白外壳、光从柱内向上推进、随机顶面细闪、浅色顶面余量及冲击波换色。主程序Release已重新链接。

30分钟日志中的310/150/110ms孤立长帧对应renderer平均CPU工作约0.05--0.07ms、资源generation始终1，且发生时既有24025柱也有50176柱；没有证据指向同步布局重建、资源泄漏或密度计算。自适应画质要求连续约2秒超预算并带冷却/回升滞回，单个系统/呈现调度停顿不会触发重建循环。该日志P95仍为19.095ms；孤立外部呈现停顿保留为硬件实机观察项，不通过无依据降低画质或关闭既定视觉效果掩盖。

主要体验按当前证据收口到约95%：同PCM分析、地形响应、核心材质、13主题静态终态、音乐事件、窗口生命周期和常用交互均有直接证据。允许略过的剩余差异为4x MSAA密集遮挡边缘极少数像素、随机粒子逐像素轨迹及原版浏览器与D3D11在极浅过渡帧的细小色差；这些不构成发布阻断。尚未把本批暂停修复重新做一轮RTX 4070固定默认密度30分钟实机长测，因此最终安装包仍需主线打包后的实际观感与长时运行确认。
