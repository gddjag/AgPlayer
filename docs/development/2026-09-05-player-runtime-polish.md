# Player runtime polish acceptance

## Scope

- Regenerate full-track waveform caches at 32,768 analysis points so the rolling 8-beat viewport keeps source detail while zooming.
- Keep endpoint/DPI mapping shared by the main and mini full-track views and invalidate sparse schema-5 caches.
- Allow the integrated/rolling waveform thumbnail column to start playback on double-click.
- Remove tag-capsule shadows and result-track export circles.
- Keep the vocal-separation primary action available to explain missing runtime/model prerequisites.
- Constrain immersive preset cards to the narrow control panel.

## Verification

- Release build: `cmake --build build/release --config Release`.
- Full Release suite: 163/163 tests passed.
- Immersive integration repeat: 20/20 consecutive runs passed.
- Chinese dark/light visual matrix: startup, playback, and mini captures passed (6 captures).
- Rolling-player QA capture completed with the fixed high-density, time-windowed waveform.
- `git diff --check` passed; line-ending notices are repository-normalization warnings only.

## Evidence

Generated evidence is under the ignored build directory:

- `build/qa/player-runtime-polish/zh-dark-playback.png`
- `build/qa/player-runtime-polish/zh-dark-mini.png`
- `build/qa/player-runtime-polish/zh-dark-rolling.png`
- Corresponding light-theme captures and `matrix.csv`

The 2-second sine fixture has nearly constant amplitude, so its waveform appears rectangular; it is used here to verify full-width endpoint coverage rather than musical shape. Source-density and endpoint behavior are covered by the waveform provider/item tests.

## Follow-up: 142-second truncation and rolling detail

The previous acceptance above covered a short fixture, not changing from a
shorter track to a longer one. A new regression reproduced `visibleEndMs`
remaining at 142000 after duration changed to 300000. Full-track views now follow
duration changes; explicit zoom/rolling ranges remain unchanged. Geometry also
rebuilds when duration changes without a viewport change.

Rolling sampling now preserves fractional source positions instead of repeatedly
rounding and stretching a temporary slice. Analysis uses 512 buckets per second,
with a 32768 minimum and 524288 maximum (8 MiB for four float layers), superseding
the fixed analysis count above. Cache schema 7 invalidates previous sparse data.
The rolling viewport remains eight beats by default, has smaller amplitude,
uniform played/unplayed brightness, and bounded right-side action buttons.
The shared list allocates 56% of its flexible title/waveform space to the title.

Fresh settings already select classic dual-window mode and continue to preserve
explicit existing choices. Actual QA startup without a shell override produced
the 863 x 266 classic window. Three-minute nonzero-tail fixtures, generated by
`scripts/qa-long-waveform.ps1`, were captured in classic and mini at scale factors
1.0, 1.25 and 1.5. Rolling was captured at 1386 x 900. Evidence is in
`build/qa/waveform-142s/`; captures verify layout/end coverage, not listening quality.

Separation volume delegates now retain identity during controller updates, so a
mouse drag does not lose its grab. Worker liveness heartbeats no longer confuse a
long native model phase with a dead process; a separate five-minute phase deadline
and cancellation remain. Added fingerprint-bound local MDX contracts for Kim
Vocal 2, Inst HQ1 and Voc FT. Unknown tensor contracts and external `.pth/.th`
backends are explicitly unavailable, not reported as configured by merely finding
ONNX Runtime. Automatic installation of those external adapters remains incomplete.

Immersive preset labels now have explicit available-width geometry and a right-side
panel; applying a preset stops song-auto-colors from immediately replacing it.
Camera-relative highlights and far-field fog use the existing QRhi renderer.
The actual D3D11 capture is `build/release/immersive-review-final.png`; its audio
features are synthetic, so it does not establish long-duration real-music quality.

Follow-up verification: Release build passed. Full CTest run passed 162/163 in
233.60 seconds; the remaining source-layout contract still required the obsolete
42% title allocation. Updated that exact contract to the newly requested 56%; its
isolated rerun passed (1/1). No application code changed after the full run.
`git diff --check` passed. The long-tail, duration-transition, fractional-pan,
volume-drag, settings, actual D3D11 and preset-label regressions are included in
this suite. Debug, additional GPU backends and long-duration listening were not
part of this follow-up validation.

Real separation evidence: Kim Vocal 2, Inst HQ1 and Voc FT each passed the
opt-in CPU native-model test (3 passed, no skips; 7.06/6.67/6.83 seconds).
The standard HTDemucs worker/IPC smoke completed in 121.7 seconds with 84
heartbeat/progress messages and five published WAV files. DirectML raised
HRESULT 80070005 during the later model phase; automatic CPU fallback completed.
This does not establish pure-GPU five-stem success. Evidence logs are
`build/release/tests/{kim,hq1,voc-ft}-real-20260905.txt` and
`build/release/tests/demucs-worker-real-20260905.txt`. These are short synthetic
audio runs, not long-song listening or end-to-end UI separation acceptance.

## Manual-test package, 2026-09-05 12:43

At the user's request, packaged the latest working-tree fixes without committing
or installing over the user's existing application. Release app and Worker were
rebuilt and their staged SHA-256 hashes matched the build outputs. The staged app
launched and played the three-minute fixture with developer Qt paths removed from
PATH; screenshot: `build/qa/waveform-142s/packaged-classic.png`.

- Desktop installer: `C:/Users/Administrator/Desktop/AgPlayer-人工测试-20260905-1243.exe`
- Version: 1.0.0.0 (timestamped manual-test package, not a new semantic release)
- Size: 35,594,025 bytes
- SHA-256: `18C389DA794B2EDF5C5CD2A6B6EDA835E2DD4A78DF0DAB31214AFE88FE882574`
- Installer compilation and desktop copy hash verification passed.
- This package retains the external-model and pure-GPU limitations documented above.

## Next feedback: centered rows, four beats and environment setup

This follow-up is source work after the 12:43 manual-test installer above; that
installer does not contain these later changes.

- New/reset frequency dimness is 30% (70% retained brightness). Existing explicit
  settings remain unchanged; fresh classic shell defaults are still preserved.
- Removed the shared full-track view's hard-coded 1-pixel black/white left mask,
  which was itself the visible vertical strip in classic and mini modes.
- Classic information rows use actual text/icon heights and consistent spacing;
  classic list title/thumbnail groups center on cover geometry.
- Single-window volume no longer disables its hover slider below 1440px; expansion
  is bounded by the available action-bar width.
- Rolling default/reset is four beats; zoom supports 2 through 64 beats. Four beats
  at 120 BPM span two seconds. A smaller time span necessarily increases horizontal
  pixel velocity at fixed window width; it does not change music playback speed.
- Rolling title/favorite, subtitle/rating and metadata rows have equal 4px gaps and
  their combined 78px block is centered on the cover. The small overview is taller,
  the main waveform uses up to 96% amplitude at the default height setting.
- A regression exposed that release previously ended rate-based scratch without
  seeking to the pointer-derived visual center. Release now seeks that exact center
  and resumes playback; cancellation does not commit a seek.

Evidence: `build/qa/waveform-142s/{classic-centered,rolling-four-beats}.png`.
Rolling QML suite: 19 cases passed after the release-coordinate fix. Settings,
mini, waveform-item and list-layout focused CTest: 4/4 passed. `all_qmllint`
completed without warnings on this snapshot. Full integration and separation
environment validation will be recorded separately when ready.

Follow-up integration: the complete 163-test run initially passed 160. The GPU
queue regression was still running an old binary; after rebuilding, the controller
suite passed. The immersive waveform test hard-coded an obsolete 0.28 setting;
it now verifies both 30% dimness / 70% brightness and live setting propagation,
restoring the prior value afterward. The editor native-drag case passed in an
isolated rerun but failed under parallel load, so that first run is not recorded
as a clean full-suite pass.

The new controller layout exposed stale MSVC object dependencies: the cached
`/showIncludes` prefix was mojibake, and the audio-tools test harness was not
recompiled when the controller header changed. Fresh configuration via the
existing `windows-msvc-release` preset and a clean full build restored dependency
tracking. The previously crashing separation QML suite then passed (3.22 seconds).
Build evidence: `build/clean-release-20260905.log`; regression evidence:
`build/separation-clean-harness.log`. Use the configure/build presets consistently
instead of mixing compiler-language environments with this build directory.

### External runtime and final feedback

- The tool-page GPU probe is queued behind directory/model verification. First
  automatic selection uses an available GPU, with explicit CPU fallback retained.
- Known `5_HP-Karaoke-UVR.pth` now has an optional, isolated Python 3.11 / PyTorch
  environment installer. The fixed uv archive is SHA-verified and reused; package
  installation reports actual stages, supports pause/resume, verifies imports and
  invalidates stale readiness while repairing. No system Python is modified.
- Independent review found and resolved cached-archive retry rejection, pause
  during archive verification, and Python CPU success incorrectly disabling the
  native GPU. The reviewer rechecked all three fixes.
- Actual Qt installer smoke (`build/external-runtime-qt-smoke.txt`): start, immediate
  pause, resume, verified readiness, then repeated repair passed in 7.3 seconds
  using the already-downloaded test cache (3 passed, no skips). This is a real
  optional installation under `build/external-runtime-probe`, not the user's
  production runtime directory.
- Real known-VR protocol run: 2-second input, 12.25-second elapsed wall time, two
  finite 44.1-kHz stereo WAV outputs. Log: `build/external-vr-real-smoke.log`.
  This adapter is CPU-only. Unknown `.th/.ckpt` architectures remain diagnosed
  rather than falsely marked runnable. Long-song pure-GPU five-stem acceptance
  remains unverified.
- Latest user addition: rolling mode no longer creates the waveform-style action.
  Both rolling views stay in frequency mode regardless of the global style; other
  player preferences are not overwritten. Regression covers all four global modes.
  Screenshot: `build/qa/waveform-142s/rolling-frequency-only.png`.

After the clean build, the full Release run passed 162/163; the only failure was
the feature-option configure check while SoundTouch was temporarily absent from
the dependency tree. Restoring the configured dependency and rerunning that check
passed. Later changes use focused reruns, not a claim of another clean 163/163 run.

Final controller check: the queued-probe regression now waits for `canStart()`
after `Idle`, then calls `start()` once. `Idle` can precede the probe worker's
process exit; production already emits the availability notification used by the
UI. Two focused probe tests passed (4 including init/cleanup, 1.102 seconds),
recorded in `build/gpu-probe-final-green.txt`. A waveform publication timeout under
parallel load passed independently in 251 ms (`build/stem-waveform-isolated.txt`);
the production timeout was not weakened. Rolling frequency-only regression passed
in `build/rolling-frequency-only-green.log`.

These results cover this feedback snapshot only. A separate frequency-algorithm
task subsequently started changing the shared main checkout; its new API and
rendering changes are not certified by this run. No new installer or commit was
created for this follow-up.

### Removed discontinued feature remnants (2026-09-05)

The user confirmed that library management had already been cancelled. A new UI
QA script had explicitly selected its dormant `library` category and mistakenly
treated the leftover page as an active feature. The page was not a new navigation
entry; the old runtime route, embedded QML and backend were nevertheless present.

- Deleted `LibraryManagerPage.qml`, its StackLayout branch, size policy, dedicated
  tests, QML resource and legacy design-system allowance. Old `library` category
  requests now resolve to the normal all-tracks list.
- Deleted `LibraryManagerController` and its maintenance scan, hash/deduplication,
  statistics, backup and management-model code. The small QObject
  `ResourceFolderController` retains only required resource-directory discovery,
  watching, import and exclusion persistence. Existing `resource-roots.json` v1
  and library serialization remain compatible; user audio and config are untouched.
- Deleted the separately discontinued `AudioFileInfoPanel`, its hidden TrackList
  helpers, `--qa-show-track-details`, dedicated hydration and QA/test references.
  Rename/tag editing still uses the existing pure metadata lookup.
- Deleted unused `tools/stage_release.ps1` and its obsolete test requirements;
  `scripts/package-windows.ps1` remains the supported packaging entry. Historical
  QA reports are retained as history, not a list of supported product features.

Verification: legacy-route RED (`build/removed-library-red.txt`) changed to GREEN
(`build/removed-library-green.txt`); 9 backend/path/storage/package-contract tests
passed in 12.33 s and 4 complete QML suites passed in 27.83 s. Release build and
`all_qmllint` passed (`build/removed-library-ui-build.log`). Independent read-only
review found no new Critical/Important issues. Native Windows screenshot with the
old category argument renders the ordinary list and exits 0:
`build/qa/removed-features/legacy-route.png`. Generated QML resource lists no longer
contain either deleted page.

The observed Release EXE decreased from 10,912,768 to 10,624,000 bytes (282 KiB).
This compares the local binaries before/after this cleanup, not installer sizes or
a controlled clean-commit benchmark. No installer was generated and no user data
was deleted. Deleted tracked source can be recovered from Git. Separate deployment
and unused-icon audits are not counted as completed work in this measurement.

### Integrated cleanup and UI/performance verification (2026-09-05)

The final 55-file UI/performance patch was integrated on top of the frequency
update `5797b9b` and the discontinued-feature cleanup. Two library-model conflicts
were resolved by retaining metadata refresh generations without restoring the
removed maintenance interface. Separate read-only backend, QML/lifecycle and
deployment reviews found no new Critical/Important issues.

- Removed 20 unused embedded resource entries and deleted the 19 unused SVG
  sources. The unembedded logo-lockup PNG remains a disk-based test fixture.
- Packaging now removes only QML tooling `.qmltypes` from its deployment stage;
  runtime QML, JavaScript, qmldir files, plugins and licenses remain. The fixture
  test verifies selective deletion, byte accounting, outside-file preservation
  and repeated execution. The separate task's controlled resource-only EXE
  comparison measured 103,424 bytes saved; its deployed tooling files totalled
  1,164,172 bytes. These are not compressed installer savings.
- Full combined Release build and `all_qmllint` passed:
  `build/integrated-polish-final-build.log`.
- A fresh complete CTest run passed **163/163 in 347.60 seconds**:
  `build/integrated-polish-full-tests.log`. This supersedes the earlier partial
  follow-up verification, and includes cleanup, frequency and UI/performance code.
- Captured classic, integrated, rolling, mini, immersive and four audio-tool
  surfaces in both dark and light themes: **18/18**, actual DPR 1, exit 0 and clean
  runtime logs. Results: `build/qa/integrated-polish-final/matrix.csv`. Representative
  shared-list, rolling, separation, converter and immersive screenshots were
  visually inspected for spacing, contrast and containment.

The screenshot audio is a short sine fixture, not long-song waveform acceptance
or subjective listening. An empty separation page in isolated QA configuration
does not verify installed user runtimes or real five-stem processing. No new
commit or installer was produced in this integration step. User music/config and
unrelated worktree changes were preserved; tracked source deletions are Git
recoverable.

Deployment follow-up: independently inspected the separate UI task's trimmed
runtime evidence under
`.worktrees/full-ui-performance-20260905/build/qa/qmltypes-smoke-d973ce1214804377bf6bdcd2ecc50ee9`.
Its runtime directory contains zero `.qmltypes`; player/settings/conversion/editor
capture results all report exit 0 and clean logs. Actual format export/reopen and
lossless-profile tests passed 4/4 including fixture setup/cleanup, native file and
color dialogs 4/4, and production conversion dialogs 3/3. This is the separate
task's standalone deployment snapshot, not a newly generated main installer.
