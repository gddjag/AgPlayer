# AgPlayer 1.0.3 macOS publication

The user confirmed manual acceptance on 2026-09-13 and authorized publication alongside Windows 1.0.3. The existing accepted DMG is published without rebuilding or modifying its bytes; only its external filename changes.

- File: `AgPlayer-1.0.3-macOS-universal.dmg`
- Size: 89,616,142 bytes
- SHA-256: `2d2bb542b5bc6927ef91745de6ca71f5a55e5d71e9bfa6adb7ece24684aa99ab`
- macOS source: `efd4d3787ebba09fafb3b23e3a885e4f5e2b9297` on `codex/macos-universal`. The existing Windows `v1.0.3` tag is unchanged.
- Build and installation evidence: GitHub Actions run `34748252708`, attempt 2. The earlier extra video-seek repeat failed intermittently; the unchanged-source retry passed. This does not establish that all playback behavior is defect-free.
- Package target: macOS 13+, Universal arm64 + x86_64. macOS 13 physical-machine validation remains pending. Automated launch checks covered ARM macOS 14, 15, 26, 27 preview and Intel macOS 15, 26; launch checks are not full UI/audio acceptance on every OS.
- Signing: ad-hoc; no Developer ID signature and no Apple notarization. Do not describe this file as Apple-approved or notarized.
- Opening guide: [Safely open apps on your Mac](https://support.apple.com/zh-cn/102445).
- Release: https://github.com/gddjag/AgPlayer/releases/tag/v1.0.3
- GitHub asset ID: `561082964`; the reported upload digest matches the accepted package.
- R2 synchronization: run `34754176669` verified and synchronized both installers. A complete public R2 download matched the accepted DMG SHA-256.

From 1.0.3 onward, Windows and macOS share version numbers and release tags. The R2 synchronization refuses shared releases missing either canonical platform installer. The download page reads both entries from the same validated manifest and keeps the previous published pair if either entry is invalid. Signing disclosures must be updated when the actual published package changes.

Release source and the identical installer are archived separately from generated builds. This record is the release-branch handoff; no cross-task message was sent because this session has no callable task-messaging tool.
